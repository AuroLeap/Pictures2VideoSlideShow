.vs
TestOut

# Rust build artifacts
/target

# Demo run artifacts (downloaded sample media + generated output)
demo/demo-media
demo/demo-output

# Local slideshow output folders (one per output definition / settings combo)
AlbumOut*

# Python bytecode (kit scripts in Scripts/)
__pycache__/
*.pyc

# Kit-generated composite artifacts (gitignored per ADOPTING.md §1).
# perf-baseline.json is deliberately NOT listed: it is the committed golden
# (process.md §9 — moving a number means committing the new baseline in the
# same PR). perf-metrics.json / perf-report.md stay ignored (regenerated
# composites).
docs/test/report.html
docs/test/stub-report.md
docs/test/perf-report.md
docs/test/perf-metrics.json
