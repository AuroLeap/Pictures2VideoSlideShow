# Architecture (one page)

Owned by the [Software Engineer persona](../../.claude/agents/software-engineer.md). The **overview** below is hand-written and must stay within one screen; the **module/function map** is generated by `scripts/trace.ps1` (Architecture section) so it cannot drift. See [process.md §4/§6](../process.md#4-traceability--anti-duplication-read-this-carefully).

Back to [docs index](README.md) · [README](../README.md).

## High-level flow (Rust engine)

```
config (TOML)               media scan (rayon)            per clip
  Config ──────────────►  MediaLoader.scan_and_index ──►  FrameSource
                              │  image dims (image)          ├─ ImageFrameSource: ClipPlan → crop/resize/rotate (rayon)
                              │  video meta  (ffprobe)        └─ VideoFrameSource: VideoFrameReader (ffmpeg decode)
                              ▼                                        │
                          Album (ordered)                             ▼
                                                          CrossfadeMixer (dissolve tail→head,
                                                          fade in first / out last)
                                                                       │ raw rgb24
                                                                       ▼
                                                          FfmpegEncoder (libx264, yuv420p) → <name>.mp4
```

## Module responsibilities

| Module | Responsibility |
|---|---|
| `src/config` | Load/validate TOML/JSON config (`OutputDef`, Ken Burns params) |
| `src/media` | Parallel scan, classify, dimensions/duration, ignore patterns |
| `src/transform` | `ClipPlan`: Ken Burns window + rotation math (deterministic) |
| `src/image` | `FrameRenderer`: pre-scale once, per-frame crop/resize/rotate |
| `src/video` | `VideoFrameReader`: stream decoded frames from ffmpeg |
| `src/ffmpeg` | `FfmpegEncoder`: stream raw rgb24 → H.264 MP4 |
| `src/pipeline` | `FrameSource` abstraction + `CrossfadeMixer` orchestration |
| `src/util` | dirs, progress |

<!-- BEGIN GENERATED MODULE MAP (scripts/trace.ps1) -->
_Generated 2026-06-02. Public items by module:_

- **src/config/mod.rs**
  - `pub struct Config`
  - `pub struct InputConfig`
  - `pub struct OutputConfig`
  - `pub struct ProcessingConfig`
  - `pub struct OutputDef`
  - `pub fn total_frames(&self) -> u32`
  - `pub fn fade_frames(&self) -> u32`
  - `pub fn from_file(path: &PathBuf) -> Result<Self>`
  - `pub fn validate(&self) -> Result<()>`
- **src/error.rs**
  - `pub enum SlideshowError`
- **src/ffmpeg/mod.rs**
  - `pub struct FfmpegEncoder`
  - `pub fn start(output: &Path, width: u32, height: u32, fps: u32, crf: u32) -> Result<Self>`
  - `pub fn write_frame(&mut self, data: &[u8]) -> Result<()>`
  - `pub fn finish(mut self) -> Result<()>`
- **src/image/mod.rs**
  - `pub struct FrameRenderer`
  - `pub fn load(path: &Path, out: &OutputDef) -> Result<Self>`
  - `pub fn total_frames(&self) -> u32`
  - `pub fn render_range(&self, start: u32, end: u32) -> Vec<Vec<u8>>`
- **src/logging.rs**
  - `pub fn init_logging(verbose: bool) -> std::io::Result<()>`
- **src/media/mod.rs**
  - `pub struct MediaFile`
  - `pub enum MediaType`
  - `pub struct Album`
  - `pub struct MediaLoader`
  - `pub fn new(config: InputConfig) -> Self`
- **src/pipeline/mod.rs**
  - `pub struct FrameGenerationPipeline`
  - `pub fn new(`
- **src/pipeline/source.rs**
  - `pub trait FrameSource`
  - `pub struct ImageFrameSource`
  - `pub fn new(renderer: FrameRenderer, batch: u32) -> Self`
  - `pub struct VideoFrameSource`
  - `pub fn new(reader: VideoFrameReader) -> Self`
- **src/transform/mod.rs**
  - `pub fn seed_from_str(s: &str) -> u64`
  - `pub struct CropWindow`
  - `pub struct ClipPlan`
  - `pub fn new(img_w: u32, img_h: u32, out: &OutputDef, seed: u64) -> Self`
  - `pub fn rotation_deg(&self, i: u32) -> f32`
  - `pub fn window(&self, i: u32) -> CropWindow`
- **src/util/file_utils.rs**
  - `pub fn ensure_dir_exists(path: &Path) -> Result<()>`
- **src/util/progress.rs**
  - `pub struct ProgressTracker`
  - `pub fn new(total: usize) -> Self`
  - `pub fn increment(&mut self)`
  - `pub fn set(&mut self, current: usize)`
- **src/video/mod.rs**
  - `pub struct VideoFrameReader`
  - `pub fn open(path: &Path, width: u32, height: u32, fps: u32) -> Result<Self>`
  - `pub fn read_frame(&mut self) -> Result<Option<Vec<u8>>>`
<!-- END GENERATED MODULE MAP -->

