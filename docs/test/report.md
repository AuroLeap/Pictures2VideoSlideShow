# Coverage & Traceability Report

_Generated by `scripts/trace.py`. Do not edit by hand._

| Metric | Count |
|---|---|
| Stakeholder needs (SN) | 31 |
| System requirements (SR) | 38 |
| Low-level requirements (LLR) | 64 |
| Test cases (TC) | 90 |
| Orphans | 0 |
| Integrity findings | 0 |
| Verified SRs — mechanized | 25 |
| Verified SRs — attested (human, §4) | 0 |
| Performance budgets (PB) | 5 |
| Budget findings | 0 |

## SR -> LLR -> TC matrix

| SR | LLRs | TCs | Status |
|---|---|---|---|
| SR-001 | LLR-027 | TC-001 | Implemented |
| SR-002 | LLR-003 LLR-004 LLR-005 | TC-002 TC-003 TC-004 TC-049 | Verified |
| SR-003 | LLR-001 LLR-002 LLR-003 LLR-044 | TC-005 TC-006 TC-007 TC-061 TC-062 | Verified |
| SR-004 | LLR-023 LLR-024 | TC-008 TC-009 TC-050 TC-051 | Verified |
| SR-005 | LLR-009 LLR-010 | TC-010 TC-011 TC-050 | Verified |
| SR-006 | LLR-010 | TC-011 TC-031 TC-046 | Verified |
| SR-007 | LLR-011 | TC-012 TC-013 | Verified |
| SR-008 | LLR-009 LLR-012 | TC-014 | Verified |
| SR-009 | LLR-013 LLR-024 | TC-009 TC-015 TC-016 TC-047 | Verified |
| SR-010 | LLR-014 | TC-017 | Verified |
| SR-011 | LLR-015 LLR-056 LLR-063 LLR-064 | TC-018 TC-080 TC-089 TC-090 | Verified |
| SR-012 | LLR-005 LLR-006 | TC-019 | Implemented |
| SR-013 | LLR-007 LLR-008 LLR-056 LLR-063 LLR-064 | TC-020 TC-021 TC-080 TC-089 TC-090 | Implemented |
| SR-014 | LLR-016 LLR-017 LLR-059 LLR-060 | TC-022 TC-023 TC-086 | Verified |
| SR-015 | LLR-015 LLR-018 LLR-056 LLR-063 LLR-064 | TC-024 TC-025 TC-048 TC-080 TC-089 TC-090 | Verified |
| SR-016 | LLR-019 | TC-003 TC-026 | Verified |
| SR-017 | LLR-020 | TC-027 TC-050 | Verified |
| SR-018 | LLR-021 LLR-059 | TC-028 | Verified |
| SR-019 | LLR-028 | TC-034 | Verified |
| SR-020 | LLR-028 | TC-035 | Verified |
| SR-021 | LLR-028 | TC-036 | Verified |
| SR-022 | LLR-022 LLR-061 LLR-062 | TC-029 TC-030 TC-087 TC-088 | Verified |
| SR-023 | LLR-025 | TC-032 | Implemented |
| SR-024 | LLR-026 | TC-033 | Draft |
| SR-025 | LLR-028 | TC-037 | Verified |
| SR-026 | LLR-029 LLR-030 | TC-038 TC-039 | Implemented |
| SR-027 | LLR-031 LLR-032 | TC-040 TC-041 | Implemented |
| SR-028 | LLR-033 | TC-042 TC-043 | Verified |
| SR-029 | LLR-034 | TC-044 | Verified |
| SR-030 | LLR-035 | TC-045 | Verified |
| SR-031 | LLR-036 LLR-037 LLR-038 LLR-044 | TC-052 TC-053 TC-054 TC-061 | Verified |
| SR-032 | LLR-039 LLR-040 LLR-041 LLR-042 LLR-057 | TC-055 TC-056 TC-057 TC-058 TC-059 TC-077 | Verified |
| SR-033 | LLR-043 LLR-044 | TC-060 TC-061 | Implemented |
| SR-034 | LLR-045 LLR-046 LLR-047 LLR-048 | TC-063 TC-064 TC-065 TC-066 TC-067 TC-068 TC-069 TC-071 | Draft |
| SR-035 | LLR-049 | TC-068 TC-070 TC-071 | Draft |
| SR-036 | LLR-050 LLR-051 LLR-052 LLR-060 LLR-061 LLR-062 LLR-063 | TC-072 TC-073 TC-074 TC-086 TC-087 TC-088 TC-089 | Draft |
| SR-037 | LLR-053 LLR-054 LLR-055 LLR-056 LLR-057 | TC-075 TC-076 TC-077 TC-078 TC-079 TC-080 TC-081 TC-082 | Draft |
| SR-038 | LLR-058 LLR-059 | TC-083 TC-084 TC-085 | Draft |

## Traceability outline

_`SN -> SR -> LLR -> TC`; `[Status]` and `[orphan]` flags are inline._

- SN-001
  - SR-001 [Implemented] — End-user download-to-first-slideshow path (no Rust)
    - LLR-027 [Implemented] — End-user download-to-first-slideshow path realized by exe + self-check/wizard + ffmpeg-resolve + config
      - TC-001 [Draft] — Demonstration on clean Windows host with no Rust toolchain and no FFmpeg: download make_video_slideshow.exe (SR-023), run it so the first-run GUI wizard (SR-026) writes a valid config (SR-030) and the startup self-check (SR-024) fetches/locates FFmpeg (SR-027), then run build; assert a playable MP4 results and no documented end-user step installs Rust/requires a source checkout/edits .rs/requires hand-editing the config
- SN-002
  - SR-002 [Verified] — Pre-run validate gates build on runtime prerequisites only
    - LLR-003 [Implemented] — Config semantic validation
      - TC-005 [Draft] — Load a config (TOML and JSON) that omits all optional fields; assert load succeeds using documented serde defaults and Config::validate passes
      - TC-007 [Draft] — Call Config::validate on configs with media_root-missing, empty outputs, zero dims, zero fps, crf>51; assert each is rejected with an InvalidConfig message
    - LLR-004 [Implemented] — Runtime-prerequisite checks with per-item plain pass/fail
      - TC-002 [Verified] — Run validate (built binary) end-to-end with all runtime prerequisites satisfied (FFmpeg/ffprobe on PATH, valid config, readable media_root, writable output base_dir); assert a labeled plain-language [PASS] line per prereq named (FFmpeg/Media root/Output), no [FAIL], and exit zero. The pass/fail aggregation->exit-code helper is unit-covered by TC-049
      - TC-003 [Verified] — Run validate (built binary) with an essential PATH prerequisite failed: (a) missing media_root -> Media root check [FAIL] naming the path; (b) output base_dir uncreatable (parent is a file) -> Output check [FAIL] naming the path; assert validate exits non-zero, no panic/stack trace. Other essential permutations (FFmpeg-missing TC-019, config-parse TC-007) and the aggregation helper (TC-049) cover the rest
      - TC-049 [Verified] — Call all_essential_passed / exit_code over check-result sets: assert exit 0 when every essential check passes (non-essential failure does not block), and exit 1 when any essential check fails. Unit aggregation portion of SR-002 (the full validate/build integration permutations are TC-002/TC-003)
    - LLR-005 [Implemented] — Build refuses to start when essential prerequisites fail
      - TC-004 [Verified] — Run build (built binary) when an essential prerequisite fails (missing media_root); assert build refuses to start, produces no output file, exits non-zero. FFmpeg-missing build leg is TC-019
      - TC-049 [Verified] — Call all_essential_passed / exit_code over check-result sets: assert exit 0 when every essential check passes (non-essential failure does not block), and exit 1 when any essential check fails. Unit aggregation portion of SR-002 (the full validate/build integration permutations are TC-002/TC-003)
- SN-003
  - SR-003 [Verified] — Single documented config file with defaults and units
    - LLR-001 [Implemented] — Single-file TOML/JSON config load with defaults
      - TC-005 [Draft] — Load a config (TOML and JSON) that omits all optional fields; assert load succeeds using documented serde defaults and Config::validate passes
    - LLR-002 [Implemented] — Config struct field set with documented defaults/units
      - TC-006 [Verified] — Inspect Quick Reference §4 and OutputDef/InputConfig/OutputConfig/ProcessingConfig structs: every user-facing field has a documented default and unit
    - LLR-003 [Implemented] — Config semantic validation
      - TC-005 [Draft] — Load a config (TOML and JSON) that omits all optional fields; assert load succeeds using documented serde defaults and Config::validate passes
      - TC-007 [Draft] — Call Config::validate on configs with media_root-missing, empty outputs, zero dims, zero fps, crf>51; assert each is rejected with an InvalidConfig message
    - LLR-044 [Implemented] — Shipped demo config + focus database are a valid configuration
      - TC-061 [Verified] — Load the shipped demo/demo_config.toml via Config::from_file and assert it passes Config::validate with exactly one even-dimension output (demo-1280x800, 1280x800, quality_crf 28, enable_audio=true) referencing focus.json; load demo/focus.json via RoiDb::load and assert two entries — the point (cat.jpg) and the bbox-centre (flower.jpg) — resolve. Guards the demo kit so a broken config/focus fails CI not an end user
    - TC-062 [Verified] — Deserialize an empty [processing] TOML table and assert the flags documented with defaults in Quick Reference §4 take them: use_parallelism=true, dry_run=false, verbose=false, ffmpeg_timeout_secs=120. Pins the SR-003 'omitted optional fields use documented defaults' contract that the shipped demo config relies on (found broken when tests/demo_assets.rs first ran: dry_run/verbose/use_parallelism had no serde defaults)
- SN-004
  - SR-004 [Verified] — Build progress and completion summary
    - LLR-023 [Implemented] — Per-stage progress reporting during build
      - TC-008 [Verified] — Run build (built binary) over a media set; capture stdout/log; assert the completion summary block (=== Build Summary ===) is emitted on completion listing the written output and the per-stage scan/encode progress is logged as work proceeds. The summary-content assertions live with TC-009; this row covers the progress-reported leg
    - LLR-024 [Implemented] — Completion summary of written outputs (and failures/oversize)
      - TC-009 [Verified] — On build success (built binary) assert the completion summary lists each written output path WITH a size and the skipped-count line; on the no-valid-output case assert the plain-language 'no outputs produced' failure message and non-zero exit (covered by TC-023 all-bad leg). Any final output >3.5GB surfaced with path+size is the oversize math TC-047 / integration TC-016
    - TC-050 [Verified] — Synthesize a tiny test video (ffmpeg testsrc=duration=1:size=320x240:rate=10) alongside a synthesized PNG into a temp media dir, run build (built binary) over the mixed image+video album; assert a non-empty output MP4 is produced. This is the only test that actually decodes a video, exercising VideoFrameReader::open/read_frame (src/video) end-to-end through the streaming mixer; doubles as the SR-004 summary check (output listed with size, skipped-count line)
    - TC-051 [Verified] — Drive the built binary through the stats and bench --images 1 subcommands; assert each exits zero and prints its key block (stats: === Project Statistics === with media/output counts; bench: === Benchmark Results === with frames/image, frame throughput, and the projection). Covers the main.rs run_stats/run_bench arms (validate/build arms covered by TC-002/TC-004/TC-008)
- SN-005
  - SR-005 [Verified] — Frame-compatible default encode profile
    - LLR-009 [Implemented] — Frame-compatible H.264/MP4 encode profile
      - TC-010 [Verified] — ffprobe a default-config output; assert codec=h264, container=mp4, pix_fmt=yuv420p, moov before mdat (faststart), width%2==0 and height%2==0, 24<=fps<=30
      - TC-014 [Verified] — Encode identical input at each CRF; assert default quality_crf is within 26-30, the configured CRF is passed unchanged to libx264 (-crf), and lowering CRF increases output file size
    - LLR-010 [Implemented] — Even-dimension enforcement for any configured resolution
      - TC-011 [Draft] — Encode at each panel resolution; ffprobe the stream; assert encoded width and height are both even and match (or even-adjusted form of) the configured values; encode never aborts on odd dims. System/ffprobe permutation only; the even_dims rounding helper is unit-covered by TC-046
      - TC-031 [Verified] — FrameRenderer::load + render_range on a synthesized image at configured dims; assert each rendered frame is exactly width*height*3 bytes (even dims honored end-to-end)
      - TC-046 [Verified] — Call OutputDef::even_dims on odd, already-even, and degenerate (1x1) configured dimensions; assert each dimension is rounded down to even (x & !1) and never falls below the even minimum of 2, so odd values can never reach yuv420p ffmpeg. Unit portion of SR-006 even-dims (the System/ffprobe permutation is TC-011)
    - TC-050 [Verified] — Synthesize a tiny test video (ffmpeg testsrc=duration=1:size=320x240:rate=10) alongside a synthesized PNG into a temp media dir, run build (built binary) over the mixed image+video album; assert a non-empty output MP4 is produced. This is the only test that actually decodes a video, exercising VideoFrameReader::open/read_frame (src/video) end-to-end through the streaming mixer; doubles as the SR-004 summary check (output listed with size, skipped-count line)
  - SR-006 [Verified] — Even-dimension enforcement for any configured resolution
    - LLR-010 [Implemented] — Even-dimension enforcement for any configured resolution
      - TC-011 [Draft] — Encode at each panel resolution; ffprobe the stream; assert encoded width and height are both even and match (or even-adjusted form of) the configured values; encode never aborts on odd dims. System/ffprobe permutation only; the even_dims rounding helper is unit-covered by TC-046
      - TC-031 [Verified] — FrameRenderer::load + render_range on a synthesized image at configured dims; assert each rendered frame is exactly width*height*3 bytes (even dims honored end-to-end)
      - TC-046 [Verified] — Call OutputDef::even_dims on odd, already-even, and degenerate (1x1) configured dimensions; assert each dimension is rounded down to even (x & !1) and never falls below the even minimum of 2, so odd values can never reach yuv420p ffmpeg. Unit portion of SR-006 even-dims (the System/ffprobe permutation is TC-011)
  - SR-007 [Verified] — Frame-rate bounds
    - LLR-011 [Implemented] — Configurable frame rate honored and default within 24-30
      - TC-012 [Verified] — Construct OutputDef and assert total_frames/fade_frames derive from configured fps and timing (fps=30, 6s display, 0.5s fade -> 180 frames, 15 fade frames)
      - TC-013 [Verified] — ffprobe default-config and configured-fps outputs; assert default 24<=fps<=30 and configured fps is honored in the encoded stream
  - SR-008 [Verified] — Quality/bitrate via CRF default
    - LLR-009 [Implemented] — Frame-compatible H.264/MP4 encode profile
      - TC-010 [Verified] — ffprobe a default-config output; assert codec=h264, container=mp4, pix_fmt=yuv420p, moov before mdat (faststart), width%2==0 and height%2==0, 24<=fps<=30
      - TC-014 [Verified] — Encode identical input at each CRF; assert default quality_crf is within 26-30, the configured CRF is passed unchanged to libx264 (-crf), and lowering CRF increases output file size
    - LLR-012 [Implemented] — Quality via CRF passthrough
      - TC-014 [Verified] — Encode identical input at each CRF; assert default quality_crf is within 26-30, the configured CRF is passed unchanged to libx264 (-crf), and lowering CRF increases output file size
  - SR-031 [Verified] — Sub-pixel-smooth Ken Burns with optional fixed focus / ROI database
    - LLR-036 [Implemented] — Sub-pixel single-warp Ken Burns render (smooth pan/zoom/rotation)
      - TC-052 [Verified] — Map a fixed source probe point through ClipPlan::projection across every frame: assert per-frame motion is bounded and at least one transition moves by a fractional (sub-pixel) amount rather than whole-pixel steps; and assert the output-region corners (the center crop kept after the rotation margin) map back inside the pre-scaled source on every frame, i.e. no black corners under rotation. Unit portion of SR-031 smooth-motion + coverage.
    - LLR-037 [Implemented] — Fixed-focus anchoring (per-output default + per-image override)
      - TC-053 [Verified] — With a fixed focus supplied to ClipPlan::with_focus (rotation off to isolate pan/zoom), map the focus source point through every frame's projection and assert it lands at a constant screen fraction (no drift) for the whole clip. Unit portion of SR-031 fixed-focus anchoring.
    - LLR-038 [Implemented] — Region-of-interest JSON database load + per-image focus resolution
      - TC-054 [Verified] — Parse a JSON ROI map (point + bbox entries) via RoiDb and resolve focuses: assert point passes through, bbox resolves to its center, backslash/case-variant paths match forward-slash keys, out-of-range coords clamp to [0,1], and invalid JSON errors; then via FrameGenerationPipeline::focus_for assert an ROI entry (keyed by path relative to media_root) overrides default_focus, a miss falls back to default_focus, and no-ROI+no-default yields None (default pan). Unit portion of SR-031 ROI database.
    - LLR-044 [Implemented] — Shipped demo config + focus database are a valid configuration
      - TC-061 [Verified] — Load the shipped demo/demo_config.toml via Config::from_file and assert it passes Config::validate with exactly one even-dimension output (demo-1280x800, 1280x800, quality_crf 28, enable_audio=true) referencing focus.json; load demo/focus.json via RoiDb::load and assert two entries — the point (cat.jpg) and the bbox-centre (flower.jpg) — resolve. Guards the demo kit so a broken config/focus fails CI not an end user
- SN-006
  - SR-009 [Verified] — Pre-run oversize (FAT32 4GB) size estimate and warning
    - LLR-013 [Implemented] — Pre-run/post-run oversize estimate and 3.5GB warning
      - TC-015 [Draft] — Configure media whose estimated output exceeds 3.5GB; assert build emits a warning naming the output and pointing to mitigations, using duration = sum(clip durations) - transitions x fade_time_secs. Integration permutation only; the estimate/oversize math is unit-covered by TC-047
      - TC-016 [Draft] — Produce a final output file >3.5GB; assert it is surfaced (path + size) in the completion summary. Integration permutation only; the oversize threshold predicate is unit-covered by TC-047
      - TC-047 [Verified] — Call estimate_output_bytes / is_oversize: assert the estimate scales linearly with duration, a lower CRF estimates a larger size, the oversize predicate is false at exactly 3.5GB and true one byte above, and a long low-CRF run is flagged oversize. Unit math portion of SR-009 (pre-run estimate TC-015 and post-run surfacing TC-016 are the integration permutations)
    - LLR-024 [Implemented] — Completion summary of written outputs (and failures/oversize)
      - TC-009 [Verified] — On build success (built binary) assert the completion summary lists each written output path WITH a size and the skipped-count line; on the no-valid-output case assert the plain-language 'no outputs produced' failure message and non-zero exit (covered by TC-023 all-bad leg). Any final output >3.5GB surfaced with path+size is the oversize math TC-047 / integration TC-016
- SN-007
  - SR-006 [Verified] — Even-dimension enforcement for any configured resolution
    - LLR-010 [Implemented] — Even-dimension enforcement for any configured resolution
      - TC-011 [Draft] — Encode at each panel resolution; ffprobe the stream; assert encoded width and height are both even and match (or even-adjusted form of) the configured values; encode never aborts on odd dims. System/ffprobe permutation only; the even_dims rounding helper is unit-covered by TC-046
      - TC-031 [Verified] — FrameRenderer::load + render_range on a synthesized image at configured dims; assert each rendered frame is exactly width*height*3 bytes (even dims honored end-to-end)
      - TC-046 [Verified] — Call OutputDef::even_dims on odd, already-even, and degenerate (1x1) configured dimensions; assert each dimension is rounded down to even (x & !1) and never falls below the even minimum of 2, so odd values can never reach yuv420p ffmpeg. Unit portion of SR-006 even-dims (the System/ffprobe permutation is TC-011)
  - SR-017 [Verified] — Multiple output definitions per run
    - LLR-020 [Implemented] — Multiple output definitions in one build
      - TC-027 [Verified] — Provide N output definitions in one config; run one build; assert N distinct MP4s written and ffprobe confirms each output's resolution/fps match its definition
    - TC-050 [Verified] — Synthesize a tiny test video (ffmpeg testsrc=duration=1:size=320x240:rate=10) alongside a synthesized PNG into a temp media dir, run build (built binary) over the mixed image+video album; assert a non-empty output MP4 is produced. This is the only test that actually decodes a video, exercising VideoFrameReader::open/read_frame (src/video) end-to-end through the streaming mixer; doubles as the SR-004 summary check (output listed with size, skipped-count line)
- SN-008
  - SR-018 [Verified] — Case-insensitive ignore list
    - LLR-021 [Implemented] — Case-insensitive ignore-pattern exclusion
      - TC-028 [Verified] — Call MediaLoader::is_ignored / scan with ignore patterns across case variants and file vs folder targets; assert matching paths are excluded case-insensitively and non-matching files included
    - LLR-059 [Draft] — Scan integration: keyed lookup vs re-probe decision
      - TC-083 [Draft] — Call the pure entry_current(entry, size, mtime) across the match matrix: both-match accepts the cached entry (hit); size-differs, mtime-differs, and both-differ each reject (re-probe). The hit/re-probe decision leg of the scan cache
      - TC-085 [Draft] — Scan a temp library cold then warm across the SR-038 entry x kind Permutations (@pairwise): hit-unchanged runs ZERO probe subprocesses/header decodes for cached entries (asserted via probe-invocation counting/logging) and the warm scan result equals the cold scan (same items, kinds, dims, durations, has_audio); miss-new (a new file) is probed and its entry cached; changed-size and changed-mtime (a resized/touched file) are re-probed and the entry replaced; corrupt-entry is discarded and re-probed without failing the scan. Skip and ignore semantics identical warm and cold — guarded by the existing TC-022/TC-023 (SR-014) and TC-028 (SR-018) suites, referenced not duplicated
- SN-009
  - SR-019 [Verified] — Honest capability/gap reporting
    - LLR-028 [Implemented] — Documentation-realized SRs (capability gaps, quick-reference, audience split)
      - TC-034 [Verified] — Inspect README/Quick Reference: bulk_video_time_min splitting is explicitly labeled not-implemented in the Rust engine and routes to the PowerShell pipeline/workaround (no doc claims it works); and no doc still lists audio as unimplemented or routes audio users away from the Rust engine (audio passthrough is implemented, SR-032)
      - TC-035 [Verified] — Inspect docs: one canonical quick-reference (commands + recommended settings) exists, is linked from README, and recommended-settings values appear in exactly one location with others linking to it
      - TC-036 [Verified] — Inspect README/roadmap/spec/quick-reference for conflicting restated constraint values; assert fps-range, crf-default, 3.5GB threshold, resolution-set are each stated once with no conflicts
      - TC-037 [Verified] — Inspect README/quick-reference: End User (run) and Developer (build-from-source) paths are distinct labeled sections; the End User quick-start has zero Rust/cargo/compiler steps; Rust build instructions appear only under Developer
  - SR-032 [Verified] — Source-video audio passthrough synced to the crossfaded timeline
    - LLR-039 [Implemented] — Per-file audio-stream probe (has_audio)
      - TC-057 [Verified] — Synthesize a tiny H.264+AAC video (ffmpeg lavfi testsrc + sine) plus a PNG into a temp album, build (built binary) with enable_audio=true, then ffprobe the output: assert it carries an aac audio stream and its container duration tracks the crossfaded timeline (image ~1s + video ~2s minus the ~0.2s dissolve). Exercises has_audio probe -> per-clip delay -> mux end to end.
      - TC-059 [Verified] — Build with enable_audio=true over an album mixing a video WITHOUT an audio stream, an image, and a video WITH audio; assert the build succeeds and ffprobe still reports an aac stream. Proves the has_audio probe excludes the silent video from the mux filtergraph (ffmpeg never errors mapping a missing :a stream) while the audio-bearing clip's track survives. The non-audio-mixed-with-audio discrimination of SR-032.
    - LLR-040 [Implemented] — Per-clip output-start capture and audio-delay math
      - TC-055 [Verified] — Call ffmpeg::audio::delay_ms over output-start frames at several fps: assert it converts a clip's output start frame to the correct adelay milliseconds (round-to-nearest), 0-frame -> 0ms, and fps=0 never panics. Pure sync-math portion of SR-032 (the end-to-end mux is TC-057).
    - LLR-041 [Implemented] — Audio mux pass (adelay/amix/AAC) with atomic finalize split
      - TC-057 [Verified] — Synthesize a tiny H.264+AAC video (ffmpeg lavfi testsrc + sine) plus a PNG into a temp album, build (built binary) with enable_audio=true, then ffprobe the output: assert it carries an aac audio stream and its container duration tracks the crossfaded timeline (image ~1s + video ~2s minus the ~0.2s dissolve). Exercises has_audio probe -> per-clip delay -> mux end to end.
      - TC-058 [Verified] — Build with enable_audio=true over an image-only album; assert a non-empty playable output is still produced and ffprobe reports NO audio stream (the silent promote fallback, not an error). Covers the no-audio-source branch of SR-032.
    - LLR-042 [Implemented] — Per-output audio config fields
      - TC-056 [Verified] — Deserialize an OutputDef TOML that omits the audio bitrate/sample-rate fields and assert they default (192 kbps / 48000 Hz) with enable_audio honored; then set explicit values and assert they round-trip through serialize/deserialize. Config-field portion of SR-032.
    - LLR-057 [Draft] — Audio-delay bookkeeping across the concat timeline
      - TC-077 [Draft] — For an album mixing images and audio-bearing video, compute each audio clip's output start frame from SegmentPlan frame offsets (cached path) and from the streaming mixer.emitted() capture (LLR-040 path); assert the two agree for every clip so warm and cold builds mux identical adelay values through the unchanged delay_ms (TC-055 math)
- SN-010
  - SR-020 [Verified] — Single discoverable quick-reference
    - LLR-028 [Implemented] — Documentation-realized SRs (capability gaps, quick-reference, audience split)
      - TC-034 [Verified] — Inspect README/Quick Reference: bulk_video_time_min splitting is explicitly labeled not-implemented in the Rust engine and routes to the PowerShell pipeline/workaround (no doc claims it works); and no doc still lists audio as unimplemented or routes audio users away from the Rust engine (audio passthrough is implemented, SR-032)
      - TC-035 [Verified] — Inspect docs: one canonical quick-reference (commands + recommended settings) exists, is linked from README, and recommended-settings values appear in exactly one location with others linking to it
      - TC-036 [Verified] — Inspect README/roadmap/spec/quick-reference for conflicting restated constraint values; assert fps-range, crf-default, 3.5GB threshold, resolution-set are each stated once with no conflicts
      - TC-037 [Verified] — Inspect README/quick-reference: End User (run) and Developer (build-from-source) paths are distinct labeled sections; the End User quick-start has zero Rust/cargo/compiler steps; Rust build instructions appear only under Developer
- SN-011
  - SR-020 [Verified] — Single discoverable quick-reference
    - LLR-028 [Implemented] — Documentation-realized SRs (capability gaps, quick-reference, audience split)
      - TC-034 [Verified] — Inspect README/Quick Reference: bulk_video_time_min splitting is explicitly labeled not-implemented in the Rust engine and routes to the PowerShell pipeline/workaround (no doc claims it works); and no doc still lists audio as unimplemented or routes audio users away from the Rust engine (audio passthrough is implemented, SR-032)
      - TC-035 [Verified] — Inspect docs: one canonical quick-reference (commands + recommended settings) exists, is linked from README, and recommended-settings values appear in exactly one location with others linking to it
      - TC-036 [Verified] — Inspect README/roadmap/spec/quick-reference for conflicting restated constraint values; assert fps-range, crf-default, 3.5GB threshold, resolution-set are each stated once with no conflicts
      - TC-037 [Verified] — Inspect README/quick-reference: End User (run) and Developer (build-from-source) paths are distinct labeled sections; the End User quick-start has zero Rust/cargo/compiler steps; Rust build instructions appear only under Developer
  - SR-021 [Verified] — No conflicting restated constraints across docs
    - LLR-028 [Implemented] — Documentation-realized SRs (capability gaps, quick-reference, audience split)
      - TC-034 [Verified] — Inspect README/Quick Reference: bulk_video_time_min splitting is explicitly labeled not-implemented in the Rust engine and routes to the PowerShell pipeline/workaround (no doc claims it works); and no doc still lists audio as unimplemented or routes audio users away from the Rust engine (audio passthrough is implemented, SR-032)
      - TC-035 [Verified] — Inspect docs: one canonical quick-reference (commands + recommended settings) exists, is linked from README, and recommended-settings values appear in exactly one location with others linking to it
      - TC-036 [Verified] — Inspect README/roadmap/spec/quick-reference for conflicting restated constraint values; assert fps-range, crf-default, 3.5GB threshold, resolution-set are each stated once with no conflicts
      - TC-037 [Verified] — Inspect README/quick-reference: End User (run) and Developer (build-from-source) paths are distinct labeled sections; the End User quick-start has zero Rust/cargo/compiler steps; Rust build instructions appear only under Developer
- SN-012
  - SR-022 [Verified] — Deterministic reproducible output
    - LLR-022 [Implemented] — Deterministic path-seeded Ken Burns motion
      - TC-029 [Verified] — Two ClipPlan::new builds over identical file path+config produce identical path-seeded zoom/pan/rotation; assert windows fit inside the prescaled image and prescaled covers output at max zoom
      - TC-030 [Verified] — Assert Ken Burns window aspect ratio matches the output aspect ratio within rounding tolerance (path-seeded, deterministic)
    - LLR-061 [Draft] — SIMD resize fast path when rotation is off
      - TC-087 [Draft] — Render the same path-seeded clip with max_rotation_degrees=0 (SIMD resize fast path) and >0 (warp_into rotation path): assert the fast path is selected iff rotation is off, both paths emit frames of the exact expected byte size, and the ClipPlan motion parameters are identical for the same seed (SR-022 determinism is plan-level; bit-exactness vs the generic resampler is NOT claimed). Existing determinism suites TC-029/TC-030/TC-052 stay green unchanged. Render-timer movement is the PB-001 evidence via TC-074
    - LLR-062 [Draft] — Fixed-point blend/scale + surplus-head clone removal
      - TC-088 [Draft] — Compare the u16 fixed-point blend/scale ((a*(256-w) + b*w + 128) >> 8) against the f32 reference over boundary and interior weights and pixel values: assert every channel of every pixel is within +-1 LSB, the zero weight returns the first frame exactly and the full weight returns the second exactly; assert the surplus-head clone in add_clip is gone (frames consumed by value). SR-022 motion determinism is untouched (weights quantize, motion parameters do not)
  - SR-031 [Verified] — Sub-pixel-smooth Ken Burns with optional fixed focus / ROI database
    - LLR-036 [Implemented] — Sub-pixel single-warp Ken Burns render (smooth pan/zoom/rotation)
      - TC-052 [Verified] — Map a fixed source probe point through ClipPlan::projection across every frame: assert per-frame motion is bounded and at least one transition moves by a fractional (sub-pixel) amount rather than whole-pixel steps; and assert the output-region corners (the center crop kept after the rotation margin) map back inside the pre-scaled source on every frame, i.e. no black corners under rotation. Unit portion of SR-031 smooth-motion + coverage.
    - LLR-037 [Implemented] — Fixed-focus anchoring (per-output default + per-image override)
      - TC-053 [Verified] — With a fixed focus supplied to ClipPlan::with_focus (rotation off to isolate pan/zoom), map the focus source point through every frame's projection and assert it lands at a constant screen fraction (no drift) for the whole clip. Unit portion of SR-031 fixed-focus anchoring.
    - LLR-038 [Implemented] — Region-of-interest JSON database load + per-image focus resolution
      - TC-054 [Verified] — Parse a JSON ROI map (point + bbox entries) via RoiDb and resolve focuses: assert point passes through, bbox resolves to its center, backslash/case-variant paths match forward-slash keys, out-of-range coords clamp to [0,1], and invalid JSON errors; then via FrameGenerationPipeline::focus_for assert an ROI entry (keyed by path relative to media_root) overrides default_focus, a miss falls back to default_focus, and no-ROI+no-default yields None (default pan). Unit portion of SR-031 ROI database.
    - LLR-044 [Implemented] — Shipped demo config + focus database are a valid configuration
      - TC-061 [Verified] — Load the shipped demo/demo_config.toml via Config::from_file and assert it passes Config::validate with exactly one even-dimension output (demo-1280x800, 1280x800, quality_crf 28, enable_audio=true) referencing focus.json; load demo/focus.json via RoiDb::load and assert two entries — the point (cat.jpg) and the bbox-centre (flower.jpg) — resolve. Guards the demo kit so a broken config/focus fails CI not an end user
- SN-013
  - SR-010 [Verified] — Source media never modified
    - LLR-014 [Implemented] — Source media opened strictly read-only
      - TC-017 [Verified] — Snapshot a fingerprint (len + mtime + FNV content hash) of every file under a temp media_root (including a nested subdir), run a full build to a SEPARATE temp output dir, then assert every input file is byte-for-byte unchanged and none added/removed; output lands outside media_root. The interrupted-mid-execution leg stays Manual (cannot deterministically kill mid-run in CI); read-only by construction (no write path to media_root) makes the complete-run leg representative
  - SR-011 [Verified] — Crash-safe output (no complete-looking partial)
    - LLR-015 [Implemented] — Atomic temp-to-final output finalize
      - TC-018 [Verified] — Drive the public FfmpegEncoder finalize: (a) a clean encode renames temp->final so <name>.mp4 exists and no .part remains; (b) a failed encode (ffmpeg exits non-zero) returns Err and leaves NEITHER <name>.mp4 NOR .part; (c) dropping the encoder mid-encode (abort/crash model) kills ffmpeg and removes .part leaving no final file; plus an end-to-end binary build success leaving <name>.mp4 and no .part
      - TC-025 [Draft] — Trigger disk-full mid-encode; assert no finalized <name>.mp4 is left for the affected output (atomic finalize)
    - LLR-056 [Draft] — Concat assembly routed through watchdog + disk-full + atomic promote
      - TC-079 [Draft] — Concat-seam spike (plan §4 mandatory early risk burn-down; first Round 3 item): build the same small fixed corpus twice with the software encoder — (a) cold/streaming, (b) segmented + `ffmpeg -f concat -c copy` assembly; ffprobe both outputs and assert identical total frame count, identical duration, and the SR-005 profile on the concat output; decode a window around every segment join and assert no dropped/duplicated frames (frame-count + per-frame hash comparison across each join). CI-runnable with the software encoder; the real-frame visual leg is TC-082
      - TC-080 [Draft] — Force failures in the concat assembly path: a non-zero concat exit surfaces as a named SlideshowError with non-zero exit and no false success; the concat child is armed with the same ffmpeg_timeout_secs watchdog (inactivity aborts, naming the output); a disk-full write maps through is_disk_full/disk_full_error naming the path; killing mid-concat leaves NO final <name>.mp4 — the final name appears only via promote() and the .part is cleaned
      - TC-082 [Draft] — Demonstration on the dev box plus a real target picture frame (plan §4 gate; scheduled BEFORE any cache implementation commit per the R2 MINOR): build the bench corpus segmented+concat with software and h264_nvenc encoders, play both outputs on the physical frame, and visually confirm seam-free playback — no stutter, flash, or timestamp jump at any segment join. Automated=No: target-hardware visual judgment
    - LLR-063 [Draft] — Encoder-writer thread + bounded channels replace batch memory budget
      - TC-089 [Draft] — Build the same album through the overlapped stage graph (prefetch -> rayon render pool -> mixer -> EncoderWriter thread over bounded sync_channels) and through the serial reference; assert the outputs match (frame count, duration, SR-005 profile via ffprobe) and that channel depth (~2x fade_frames) bounds memory in place of the deleted RANGE_MEMORY_BUDGET (peak working set is the PB-005 measurement via TC-074). The existing behavioral suites must stay green unchanged — TC-018 (atomic finalize), TC-021 (watchdog), TC-022/TC-023 (skip), TC-057/TC-058/TC-059 (audio) — referenced, not duplicated
    - LLR-064 [Draft] — Cross-thread failure propagation preserves exit/cleanup semantics
      - TC-090 [Draft] — Inject writer-thread failures into the overlapped pipeline — ffmpeg non-zero exit, broken pipe, disk-full (is_disk_full mapping), watchdog kill — and assert EncoderWriter::join re-raises each in encode_output with serial semantics: non-zero exit, no false success, encoder Drop kills ffmpeg and removes the .part leaving NO final <name>.mp4, and upstream stages unblock via channel disconnect within bounded time (no deadlock)
- SN-014
  - SR-012 [Implemented] — FFmpeg-missing detection
    - LLR-005 [Implemented] — Build refuses to start when essential prerequisites fail
      - TC-004 [Verified] — Run build (built binary) when an essential prerequisite fails (missing media_root); assert build refuses to start, produces no output file, exits non-zero. FFmpeg-missing build leg is TC-019
      - TC-049 [Verified] — Call all_essential_passed / exit_code over check-result sets: assert exit 0 when every essential check passes (non-essential failure does not block), and exit 1 when any essential check fails. Unit aggregation portion of SR-002 (the full validate/build integration permutations are TC-002/TC-003)
    - LLR-006 [Implemented] — FFmpeg-missing detection with named plain-language error
      - TC-019 [Draft] — With FFmpeg absent from PATH, run validate and build; assert each prints an FFmpeg-not-found message with a fix hint, produces no output, exits non-zero. Manual/Demonstration: cannot deterministically remove FFmpeg from a shared PATH in CI/local where FFmpeg is installed, so the absent-FFmpeg end-to-end message is a human demo; the resolve/None logic (FFmpeg-not-locatable) is unit-covered by cargo test ffmpeg::resolve::tests::pick_none_when_no_source_sr027 and the preflight check_program path is exercised by validate_build_checks
  - SR-013 [Implemented] — FFmpeg-error handling with no false success
    - LLR-007 [Implemented] — FFmpeg non-zero exit surfaced as error, no false success
      - TC-020 [Draft] — Drive FfmpegEncoder::finish on a child that exits non-zero; assert SlideshowError::Ffmpeg with the exit status is returned (no false success); assert write_frame propagates broken-pipe
    - LLR-008 [Implemented] — Encode failure names affected output and bounds run time
      - TC-021 [Draft] — Force a non-zero FFmpeg exit / inactivity during an output; assert the error names the failing output/file, exits non-zero, lists no written file for that output, and terminates within a bounded time (no indefinite hang). Manual/Demonstration: a deterministic inactivity-timeout end-to-end requires wedging ffmpeg and waiting out a real interval (flaky in CI); the bounded-wait DECISION is unit-covered by cargo test ffmpeg::tests::inactivity_timeout_decision_sr013 (timed_out), and the no-final-file-on-nonzero-exit leg is integration-covered by TC-018 (failed_encode_leaves_no_final_and_cleans_part_sr011_sr015). timeout_secs is configurable so a manual demo can use a tiny value
    - LLR-056 [Draft] — Concat assembly routed through watchdog + disk-full + atomic promote
      - TC-079 [Draft] — Concat-seam spike (plan §4 mandatory early risk burn-down; first Round 3 item): build the same small fixed corpus twice with the software encoder — (a) cold/streaming, (b) segmented + `ffmpeg -f concat -c copy` assembly; ffprobe both outputs and assert identical total frame count, identical duration, and the SR-005 profile on the concat output; decode a window around every segment join and assert no dropped/duplicated frames (frame-count + per-frame hash comparison across each join). CI-runnable with the software encoder; the real-frame visual leg is TC-082
      - TC-080 [Draft] — Force failures in the concat assembly path: a non-zero concat exit surfaces as a named SlideshowError with non-zero exit and no false success; the concat child is armed with the same ffmpeg_timeout_secs watchdog (inactivity aborts, naming the output); a disk-full write maps through is_disk_full/disk_full_error naming the path; killing mid-concat leaves NO final <name>.mp4 — the final name appears only via promote() and the .part is cleaned
      - TC-082 [Draft] — Demonstration on the dev box plus a real target picture frame (plan §4 gate; scheduled BEFORE any cache implementation commit per the R2 MINOR): build the bench corpus segmented+concat with software and h264_nvenc encoders, play both outputs on the physical frame, and visually confirm seam-free playback — no stutter, flash, or timestamp jump at any segment join. Automated=No: target-hardware visual judgment
    - LLR-063 [Draft] — Encoder-writer thread + bounded channels replace batch memory budget
      - TC-089 [Draft] — Build the same album through the overlapped stage graph (prefetch -> rayon render pool -> mixer -> EncoderWriter thread over bounded sync_channels) and through the serial reference; assert the outputs match (frame count, duration, SR-005 profile via ffprobe) and that channel depth (~2x fade_frames) bounds memory in place of the deleted RANGE_MEMORY_BUDGET (peak working set is the PB-005 measurement via TC-074). The existing behavioral suites must stay green unchanged — TC-018 (atomic finalize), TC-021 (watchdog), TC-022/TC-023 (skip), TC-057/TC-058/TC-059 (audio) — referenced, not duplicated
    - LLR-064 [Draft] — Cross-thread failure propagation preserves exit/cleanup semantics
      - TC-090 [Draft] — Inject writer-thread failures into the overlapped pipeline — ffmpeg non-zero exit, broken pipe, disk-full (is_disk_full mapping), watchdog kill — and assert EncoderWriter::join re-raises each in encode_output with serial semantics: non-zero exit, no false success, encoder Drop kills ffmpeg and removes the .part leaving NO final <name>.mp4, and upstream stages unblock via channel disconnect within bounded time (no deadlock)
- SN-015
  - SR-014 [Verified] — Corrupt/unsupported input skip-and-continue
    - LLR-016 [Implemented] — Skip-and-continue on unreadable/unsupported input
      - TC-022 [Verified] — Build (built binary) over a media set mixing a corrupt image (valid PNG header so it scans, truncated body so full decode fails) with a valid image; assert the bad file is skipped, logged with path+reason in the summary, and the valid file still produces a non-empty output
    - LLR-017 [Implemented] — Skipped-count and per-skip reason in completion summary
      - TC-023 [Verified] — some-bad vs all-bad input: some-bad asserts the summary states skipped-count>=1 and with >=1 valid output the run exits zero (cargo test --test skip_and_continue build_skips_bad_and_continues_exit_zero_sr014, Verified). all-bad asserts every-input-skipped -> non-zero exit + plain 'no outputs produced' message + no MP4 (cargo test --test skip_and_continue build_all_bad_exits_nonzero_no_outputs_sr014) -- SR-014 fix landed in pipeline::encode_output (produced_total==0 drops the mixer without finalizing so no empty MP4 is written and build exits non-zero); test un-ignored and passing
    - LLR-059 [Draft] — Scan integration: keyed lookup vs re-probe decision
      - TC-083 [Draft] — Call the pure entry_current(entry, size, mtime) across the match matrix: both-match accepts the cached entry (hit); size-differs, mtime-differs, and both-differ each reject (re-probe). The hit/re-probe decision leg of the scan cache
      - TC-085 [Draft] — Scan a temp library cold then warm across the SR-038 entry x kind Permutations (@pairwise): hit-unchanged runs ZERO probe subprocesses/header decodes for cached entries (asserted via probe-invocation counting/logging) and the warm scan result equals the cold scan (same items, kinds, dims, durations, has_audio); miss-new (a new file) is probed and its entry cached; changed-size and changed-mtime (a resized/touched file) are re-probed and the entry replaced; corrupt-entry is discarded and re-probed without failing the scan. Skip and ignore semantics identical warm and cold — guarded by the existing TC-022/TC-023 (SR-014) and TC-028 (SR-018) suites, referenced not duplicated
    - LLR-060 [Draft] — Background clip prefetch with bounded lookahead
      - TC-086 [Draft] — With background clip prefetch active (bounded lookahead 1-2), build over an album containing a corrupt clip among valid ones: assert the failed prefetch Result is delivered IN ORDER to the clip loop and routed through record_skip so the skipped count, per-skip path+reason, and exit semantics are identical to the serial path; the existing SR-014 suites (TC-022 skip-and-continue, TC-023 some-bad/all-bad exits) must stay green unchanged — referenced, not duplicated. Boundary-stall reduction is the PB-002 measurement via TC-074's bench run, not asserted here
- SN-016
  - SR-011 [Verified] — Crash-safe output (no complete-looking partial)
    - LLR-015 [Implemented] — Atomic temp-to-final output finalize
      - TC-018 [Verified] — Drive the public FfmpegEncoder finalize: (a) a clean encode renames temp->final so <name>.mp4 exists and no .part remains; (b) a failed encode (ffmpeg exits non-zero) returns Err and leaves NEITHER <name>.mp4 NOR .part; (c) dropping the encoder mid-encode (abort/crash model) kills ffmpeg and removes .part leaving no final file; plus an end-to-end binary build success leaving <name>.mp4 and no .part
      - TC-025 [Draft] — Trigger disk-full mid-encode; assert no finalized <name>.mp4 is left for the affected output (atomic finalize)
    - LLR-056 [Draft] — Concat assembly routed through watchdog + disk-full + atomic promote
      - TC-079 [Draft] — Concat-seam spike (plan §4 mandatory early risk burn-down; first Round 3 item): build the same small fixed corpus twice with the software encoder — (a) cold/streaming, (b) segmented + `ffmpeg -f concat -c copy` assembly; ffprobe both outputs and assert identical total frame count, identical duration, and the SR-005 profile on the concat output; decode a window around every segment join and assert no dropped/duplicated frames (frame-count + per-frame hash comparison across each join). CI-runnable with the software encoder; the real-frame visual leg is TC-082
      - TC-080 [Draft] — Force failures in the concat assembly path: a non-zero concat exit surfaces as a named SlideshowError with non-zero exit and no false success; the concat child is armed with the same ffmpeg_timeout_secs watchdog (inactivity aborts, naming the output); a disk-full write maps through is_disk_full/disk_full_error naming the path; killing mid-concat leaves NO final <name>.mp4 — the final name appears only via promote() and the .part is cleaned
      - TC-082 [Draft] — Demonstration on the dev box plus a real target picture frame (plan §4 gate; scheduled BEFORE any cache implementation commit per the R2 MINOR): build the bench corpus segmented+concat with software and h264_nvenc encoders, play both outputs on the physical frame, and visually confirm seam-free playback — no stutter, flash, or timestamp jump at any segment join. Automated=No: target-hardware visual judgment
    - LLR-063 [Draft] — Encoder-writer thread + bounded channels replace batch memory budget
      - TC-089 [Draft] — Build the same album through the overlapped stage graph (prefetch -> rayon render pool -> mixer -> EncoderWriter thread over bounded sync_channels) and through the serial reference; assert the outputs match (frame count, duration, SR-005 profile via ffprobe) and that channel depth (~2x fade_frames) bounds memory in place of the deleted RANGE_MEMORY_BUDGET (peak working set is the PB-005 measurement via TC-074). The existing behavioral suites must stay green unchanged — TC-018 (atomic finalize), TC-021 (watchdog), TC-022/TC-023 (skip), TC-057/TC-058/TC-059 (audio) — referenced, not duplicated
    - LLR-064 [Draft] — Cross-thread failure propagation preserves exit/cleanup semantics
      - TC-090 [Draft] — Inject writer-thread failures into the overlapped pipeline — ffmpeg non-zero exit, broken pipe, disk-full (is_disk_full mapping), watchdog kill — and assert EncoderWriter::join re-raises each in encode_output with serial semantics: non-zero exit, no false success, encoder Drop kills ffmpeg and removes the .part leaving NO final <name>.mp4, and upstream stages unblock via channel disconnect within bounded time (no deadlock)
  - SR-015 [Verified] — Storage-full detection without complete-looking output
    - LLR-015 [Implemented] — Atomic temp-to-final output finalize
      - TC-018 [Verified] — Drive the public FfmpegEncoder finalize: (a) a clean encode renames temp->final so <name>.mp4 exists and no .part remains; (b) a failed encode (ffmpeg exits non-zero) returns Err and leaves NEITHER <name>.mp4 NOR .part; (c) dropping the encoder mid-encode (abort/crash model) kills ffmpeg and removes .part leaving no final file; plus an end-to-end binary build success leaving <name>.mp4 and no .part
      - TC-025 [Draft] — Trigger disk-full mid-encode; assert no finalized <name>.mp4 is left for the affected output (atomic finalize)
    - LLR-018 [Implemented] — Disk-full write failure detected and named
      - TC-024 [Draft] — Constrain the output volume and the temp volume so writes fail with a real ENOSPC/ERROR_DISK_FULL; assert an out-of-space message naming the path, non-zero exit, and no file left with the final output name. Integration permutation only (real full volume); the is_disk_full classification helper is unit-covered by TC-048
      - TC-048 [Verified] — Call is_disk_full on the platform out-of-space code (Windows ERROR_DISK_FULL=112 / POSIX ENOSPC=28) vs other errors (NotFound, access-denied); assert only the platform disk-full code classifies true. Unit portion of SR-015 disk-full classification (the real-ENOSPC end-to-end run is TC-024)
    - LLR-056 [Draft] — Concat assembly routed through watchdog + disk-full + atomic promote
      - TC-079 [Draft] — Concat-seam spike (plan §4 mandatory early risk burn-down; first Round 3 item): build the same small fixed corpus twice with the software encoder — (a) cold/streaming, (b) segmented + `ffmpeg -f concat -c copy` assembly; ffprobe both outputs and assert identical total frame count, identical duration, and the SR-005 profile on the concat output; decode a window around every segment join and assert no dropped/duplicated frames (frame-count + per-frame hash comparison across each join). CI-runnable with the software encoder; the real-frame visual leg is TC-082
      - TC-080 [Draft] — Force failures in the concat assembly path: a non-zero concat exit surfaces as a named SlideshowError with non-zero exit and no false success; the concat child is armed with the same ffmpeg_timeout_secs watchdog (inactivity aborts, naming the output); a disk-full write maps through is_disk_full/disk_full_error naming the path; killing mid-concat leaves NO final <name>.mp4 — the final name appears only via promote() and the .part is cleaned
      - TC-082 [Draft] — Demonstration on the dev box plus a real target picture frame (plan §4 gate; scheduled BEFORE any cache implementation commit per the R2 MINOR): build the bench corpus segmented+concat with software and h264_nvenc encoders, play both outputs on the physical frame, and visually confirm seam-free playback — no stutter, flash, or timestamp jump at any segment join. Automated=No: target-hardware visual judgment
    - LLR-063 [Draft] — Encoder-writer thread + bounded channels replace batch memory budget
      - TC-089 [Draft] — Build the same album through the overlapped stage graph (prefetch -> rayon render pool -> mixer -> EncoderWriter thread over bounded sync_channels) and through the serial reference; assert the outputs match (frame count, duration, SR-005 profile via ffprobe) and that channel depth (~2x fade_frames) bounds memory in place of the deleted RANGE_MEMORY_BUDGET (peak working set is the PB-005 measurement via TC-074). The existing behavioral suites must stay green unchanged — TC-018 (atomic finalize), TC-021 (watchdog), TC-022/TC-023 (skip), TC-057/TC-058/TC-059 (audio) — referenced, not duplicated
    - LLR-064 [Draft] — Cross-thread failure propagation preserves exit/cleanup semantics
      - TC-090 [Draft] — Inject writer-thread failures into the overlapped pipeline — ffmpeg non-zero exit, broken pipe, disk-full (is_disk_full mapping), watchdog kill — and assert EncoderWriter::join re-raises each in encode_output with serial semantics: non-zero exit, no false success, encoder Drop kills ffmpeg and removes the .part leaving NO final <name>.mp4, and upstream stages unblock via channel disconnect within bounded time (no deadlock)
- SN-017
  - SR-016 [Verified] — Output/input path missing or unwritable handling
    - LLR-019 [Implemented] — Output/input path missing-or-unwritable detection
      - TC-003 [Verified] — Run validate (built binary) with an essential PATH prerequisite failed: (a) missing media_root -> Media root check [FAIL] naming the path; (b) output base_dir uncreatable (parent is a file) -> Output check [FAIL] naming the path; assert validate exits non-zero, no panic/stack trace. Other essential permutations (FFmpeg-missing TC-019, config-parse TC-007) and the aggregation helper (TC-049) cover the rest
      - TC-026 [Verified] — Pre-run (built binary): with media_root missing and with output base_dir uncreatable (parent is a file), assert the exact path and problem are named in plain language, non-zero exit, and no panic/stack trace (asserted explicitly). The mid-run/read-only-after-start and real-removed-volume permutations stay Manual/Demonstration (cannot deterministically yank a volume mid-encode in CI)
- SN-018
  - SR-009 [Verified] — Pre-run oversize (FAT32 4GB) size estimate and warning
    - LLR-013 [Implemented] — Pre-run/post-run oversize estimate and 3.5GB warning
      - TC-015 [Draft] — Configure media whose estimated output exceeds 3.5GB; assert build emits a warning naming the output and pointing to mitigations, using duration = sum(clip durations) - transitions x fade_time_secs. Integration permutation only; the estimate/oversize math is unit-covered by TC-047
      - TC-016 [Draft] — Produce a final output file >3.5GB; assert it is surfaced (path + size) in the completion summary. Integration permutation only; the oversize threshold predicate is unit-covered by TC-047
      - TC-047 [Verified] — Call estimate_output_bytes / is_oversize: assert the estimate scales linearly with duration, a lower CRF estimates a larger size, the oversize predicate is false at exactly 3.5GB and true one byte above, and a long low-CRF run is flagged oversize. Unit math portion of SR-009 (pre-run estimate TC-015 and post-run surfacing TC-016 are the integration permutations)
    - LLR-024 [Implemented] — Completion summary of written outputs (and failures/oversize)
      - TC-009 [Verified] — On build success (built binary) assert the completion summary lists each written output path WITH a size and the skipped-count line; on the no-valid-output case assert the plain-language 'no outputs produced' failure message and non-zero exit (covered by TC-023 all-bad leg). Any final output >3.5GB surfaced with path+size is the oversize math TC-047 / integration TC-016
- SN-019
  - SR-001 [Implemented] — End-user download-to-first-slideshow path (no Rust)
    - LLR-027 [Implemented] — End-user download-to-first-slideshow path realized by exe + self-check/wizard + ffmpeg-resolve + config
      - TC-001 [Draft] — Demonstration on clean Windows host with no Rust toolchain and no FFmpeg: download make_video_slideshow.exe (SR-023), run it so the first-run GUI wizard (SR-026) writes a valid config (SR-030) and the startup self-check (SR-024) fetches/locates FFmpeg (SR-027), then run build; assert a playable MP4 results and no documented end-user step installs Rust/requires a source checkout/edits .rs/requires hand-editing the config
  - SR-023 [Implemented] — Prebuilt binary distribution via CI to GitHub Releases (FFmpeg not bundled)
    - LLR-025 [Implemented] — CI-built make_video_slideshow.exe published to GitHub Releases on tag
      - TC-032 [Draft] — Demonstration/CI: assert the CI release workflow (on a release tag) builds and publishes a make_video_slideshow.exe asset to GitHub Releases and that the published artifact bundles/contains no FFmpeg; then on a clean Windows host with NO Rust/build tools, download that make_video_slideshow.exe and run validate then build, using only the binary + an FFmpeg obtained per SR-027; assert a playable MP4 is produced and no .NET/Rust runtime install is required
- SN-020
  - SR-001 [Implemented] — End-user download-to-first-slideshow path (no Rust)
    - LLR-027 [Implemented] — End-user download-to-first-slideshow path realized by exe + self-check/wizard + ffmpeg-resolve + config
      - TC-001 [Draft] — Demonstration on clean Windows host with no Rust toolchain and no FFmpeg: download make_video_slideshow.exe (SR-023), run it so the first-run GUI wizard (SR-026) writes a valid config (SR-030) and the startup self-check (SR-024) fetches/locates FFmpeg (SR-027), then run build; assert a playable MP4 results and no documented end-user step installs Rust/requires a source checkout/edits .rs/requires hand-editing the config
  - SR-024 [Draft] — In-binary startup dependency self-check and first-run wizard launch
    - LLR-026 [Implemented] — In-binary startup self-check + first-run-no-config wizard launch (no standalone setup script)
      - TC-033 [Draft] — Launch make_video_slideshow.exe on a clean Windows host across the config-present x ffmpeg-present matrix in an interactive session; assert every launch runs a dependency self-check reporting in plain language whether FFmpeg is locatable, that when FFmpeg is missing it prompts to fetch/locate it (SR-027), that on first run with no config it launches the GUI wizard (SR-026) before proceeding, and that the self-check requires no Rust and runs on a clean host
- SN-021
  - SR-001 [Implemented] — End-user download-to-first-slideshow path (no Rust)
    - LLR-027 [Implemented] — End-user download-to-first-slideshow path realized by exe + self-check/wizard + ffmpeg-resolve + config
      - TC-001 [Draft] — Demonstration on clean Windows host with no Rust toolchain and no FFmpeg: download make_video_slideshow.exe (SR-023), run it so the first-run GUI wizard (SR-026) writes a valid config (SR-030) and the startup self-check (SR-024) fetches/locates FFmpeg (SR-027), then run build; assert a playable MP4 results and no documented end-user step installs Rust/requires a source checkout/edits .rs/requires hand-editing the config
  - SR-025 [Verified] — End User vs Developer documentation audience split
    - LLR-028 [Implemented] — Documentation-realized SRs (capability gaps, quick-reference, audience split)
      - TC-034 [Verified] — Inspect README/Quick Reference: bulk_video_time_min splitting is explicitly labeled not-implemented in the Rust engine and routes to the PowerShell pipeline/workaround (no doc claims it works); and no doc still lists audio as unimplemented or routes audio users away from the Rust engine (audio passthrough is implemented, SR-032)
      - TC-035 [Verified] — Inspect docs: one canonical quick-reference (commands + recommended settings) exists, is linked from README, and recommended-settings values appear in exactly one location with others linking to it
      - TC-036 [Verified] — Inspect README/roadmap/spec/quick-reference for conflicting restated constraint values; assert fps-range, crf-default, 3.5GB threshold, resolution-set are each stated once with no conflicts
      - TC-037 [Verified] — Inspect README/quick-reference: End User (run) and Developer (build-from-source) paths are distinct labeled sections; the End User quick-start has zero Rust/cargo/compiler steps; Rust build instructions appear only under Developer
- SN-022
  - SR-026 [Implemented] — First-run GUI wizard collects settings and writes a valid config
    - LLR-029 [Implemented] — First-run GUI wizard (PowerShell/WinForms) collects setup inputs
      - TC-038 [Draft] — Demonstration: on first run with no config in an interactive session, launch make_video_slideshow.exe; assert the GUI wizard (PowerShell/WinForms) appears, collects source path, intermediary/temp path, frame defaults (resolution/fps/quality) and single-vs-multiple-frames, and on completion the run proceeds with the new config; assert the wizard appears only in interactive use (SR-028)
    - LLR-030 [Implemented] — Pure wizard-values→valid Config/TOML mapping (unit-testable, no GUI)
      - TC-039 [Verified] — Call the pure build_config mapping on a set of wizard-collected values (no GUI); assert the produced Config serializes to TOML that parses via Config::from_file and passes Config::validate (SR-002/SR-003), each collected field landing in its documented config field; assert the single-frames choice yields one [[outputs]] and the multiple-frames choice yields multiple
- SN-023
  - SR-027 [Implemented] — Auto-fetch FFmpeg to per-user dir with offline/existing fallback
    - LLR-031 [Implemented] — FFmpeg auto-fetch to per-user dir with offline/use-existing fallback
      - TC-040 [Draft] — Drive the FFmpeg auto-fetch/fallback across the three states; assert online-fetch downloads FFmpeg into a per-user dir without elevation, verifies it (SR-029) and then locates it for validate/build; assert offline-existing uses a user-supplied/configured FFmpeg path with no download; assert offline-none does not silently proceed but reports FFmpeg-unavailable in plain language and exits non-zero; assert no state requires administrator rights
    - LLR-032 [Implemented] — FFmpeg resolution-order helper (PATH/configured → per-user cache → fetch)
      - TC-041 [Verified] — Call the resolve_ffmpeg resolution-order helper with the binary present in each position; assert the documented precedence PATH/configured-existing first, then the per-user auto-fetch cache, then trigger a fetch (when interactive/permitted), returning the first resolved path with no duplicated probing
- SN-024
  - SR-028 [Verified] — Interactive-only prompts; non-interactive path never blocks
    - LLR-033 [Implemented] — Interactive vs non-interactive gating (never prompt/GUI when non-interactive)
      - TC-042 [Draft] — Harness guardrail across interactive states; assert that a non-interactive invocation (--config <valid> or --non-interactive, or no attached console) runs validate/build to completion with NO GUI/prompt shown and no wait for input, that a non-interactive run lacking a usable config/dependency exits non-zero with a plain message rather than launching a GUI or blocking, and that under CI/no-console every invocation terminates within bounded time and never opens a dialog
      - TC-043 [Verified] — Call the is_interactive predicate with TTY/attached-console present vs absent and with --non-interactive / --config <path> set; assert it returns false (never prompt/GUI) in every non-interactive/CI case so the gated wizard (LLR-029) and fetch prompt (LLR-031) are suppressed
- SN-025
  - SR-030 [Verified] — Config-write location precedence reported and re-read
    - LLR-035 [Implemented] — Config-write location resolver (beside-exe if writable else %APPDATA%), reported and re-read
      - TC-045 [Verified] — Call resolve_config_path with the exe's own directory writable vs read-only; assert the config is written beside the exe when writable and under %APPDATA% when read-only, that the chosen path is reported in plain language, and that a subsequent run re-reads from the same precedence (beside-exe first, then %APPDATA%) without re-prompting/re-running the wizard
- SN-026
  - SR-029 [Verified] — Downloaded FFmpeg integrity-verified against pinned checksum
    - LLR-034 [Implemented] — Fetched-FFmpeg integrity verification against pinned checksum
      - TC-044 [Verified] — Call verify_checksum on a fetched FFmpeg whose checksum matches the pinned/trusted value vs one that mismatches (or is missing/corrupt); assert the match case permits use and the mismatch case emits a plain-language integrity-failure message, never executes the suspect binary, and aborts non-zero; assert the pinned checksum is recorded in the binary/config, not fetched unauthenticated alongside the download
- SN-027
  - SR-031 [Verified] — Sub-pixel-smooth Ken Burns with optional fixed focus / ROI database
    - LLR-036 [Implemented] — Sub-pixel single-warp Ken Burns render (smooth pan/zoom/rotation)
      - TC-052 [Verified] — Map a fixed source probe point through ClipPlan::projection across every frame: assert per-frame motion is bounded and at least one transition moves by a fractional (sub-pixel) amount rather than whole-pixel steps; and assert the output-region corners (the center crop kept after the rotation margin) map back inside the pre-scaled source on every frame, i.e. no black corners under rotation. Unit portion of SR-031 smooth-motion + coverage.
    - LLR-037 [Implemented] — Fixed-focus anchoring (per-output default + per-image override)
      - TC-053 [Verified] — With a fixed focus supplied to ClipPlan::with_focus (rotation off to isolate pan/zoom), map the focus source point through every frame's projection and assert it lands at a constant screen fraction (no drift) for the whole clip. Unit portion of SR-031 fixed-focus anchoring.
    - LLR-038 [Implemented] — Region-of-interest JSON database load + per-image focus resolution
      - TC-054 [Verified] — Parse a JSON ROI map (point + bbox entries) via RoiDb and resolve focuses: assert point passes through, bbox resolves to its center, backslash/case-variant paths match forward-slash keys, out-of-range coords clamp to [0,1], and invalid JSON errors; then via FrameGenerationPipeline::focus_for assert an ROI entry (keyed by path relative to media_root) overrides default_focus, a miss falls back to default_focus, and no-ROI+no-default yields None (default pan). Unit portion of SR-031 ROI database.
    - LLR-044 [Implemented] — Shipped demo config + focus database are a valid configuration
      - TC-061 [Verified] — Load the shipped demo/demo_config.toml via Config::from_file and assert it passes Config::validate with exactly one even-dimension output (demo-1280x800, 1280x800, quality_crf 28, enable_audio=true) referencing focus.json; load demo/focus.json via RoiDb::load and assert two entries — the point (cat.jpg) and the bbox-centre (flower.jpg) — resolve. Guards the demo kit so a broken config/focus fails CI not an end user
- SN-028
  - SR-032 [Verified] — Source-video audio passthrough synced to the crossfaded timeline
    - LLR-039 [Implemented] — Per-file audio-stream probe (has_audio)
      - TC-057 [Verified] — Synthesize a tiny H.264+AAC video (ffmpeg lavfi testsrc + sine) plus a PNG into a temp album, build (built binary) with enable_audio=true, then ffprobe the output: assert it carries an aac audio stream and its container duration tracks the crossfaded timeline (image ~1s + video ~2s minus the ~0.2s dissolve). Exercises has_audio probe -> per-clip delay -> mux end to end.
      - TC-059 [Verified] — Build with enable_audio=true over an album mixing a video WITHOUT an audio stream, an image, and a video WITH audio; assert the build succeeds and ffprobe still reports an aac stream. Proves the has_audio probe excludes the silent video from the mux filtergraph (ffmpeg never errors mapping a missing :a stream) while the audio-bearing clip's track survives. The non-audio-mixed-with-audio discrimination of SR-032.
    - LLR-040 [Implemented] — Per-clip output-start capture and audio-delay math
      - TC-055 [Verified] — Call ffmpeg::audio::delay_ms over output-start frames at several fps: assert it converts a clip's output start frame to the correct adelay milliseconds (round-to-nearest), 0-frame -> 0ms, and fps=0 never panics. Pure sync-math portion of SR-032 (the end-to-end mux is TC-057).
    - LLR-041 [Implemented] — Audio mux pass (adelay/amix/AAC) with atomic finalize split
      - TC-057 [Verified] — Synthesize a tiny H.264+AAC video (ffmpeg lavfi testsrc + sine) plus a PNG into a temp album, build (built binary) with enable_audio=true, then ffprobe the output: assert it carries an aac audio stream and its container duration tracks the crossfaded timeline (image ~1s + video ~2s minus the ~0.2s dissolve). Exercises has_audio probe -> per-clip delay -> mux end to end.
      - TC-058 [Verified] — Build with enable_audio=true over an image-only album; assert a non-empty playable output is still produced and ffprobe reports NO audio stream (the silent promote fallback, not an error). Covers the no-audio-source branch of SR-032.
    - LLR-042 [Implemented] — Per-output audio config fields
      - TC-056 [Verified] — Deserialize an OutputDef TOML that omits the audio bitrate/sample-rate fields and assert they default (192 kbps / 48000 Hz) with enable_audio honored; then set explicit values and assert they round-trip through serialize/deserialize. Config-field portion of SR-032.
    - LLR-057 [Draft] — Audio-delay bookkeeping across the concat timeline
      - TC-077 [Draft] — For an album mixing images and audio-bearing video, compute each audio clip's output start frame from SegmentPlan frame offsets (cached path) and from the streaming mixer.emitted() capture (LLR-040 path); assert the two agree for every clip so warm and cold builds mux identical adelay values through the unchanged delay_ms (TC-055 math)
- SN-029
  - SR-033 [Implemented] — Bundled one-click demo (sample media + worked config/focus) produces a slideshow
    - LLR-043 [Implemented] — demo.bat + bundled demo config/focus + release-zip packaging
      - TC-060 [Draft] — Demonstration on a clean Windows host: from an extracted release folder (make_video_slideshow.exe + demo.bat + demo_config.toml + focus.json) run demo.bat; assert it downloads the pinned free-use sample media, verifies each SHA-256, auto-fetches FFmpeg if missing, runs validate then build, and writes a playable MP4 to demo-output; then assert the exe-absent path prints a named actionable error and exits non-zero WITHOUT downloading, and a pinned-checksum mismatch deletes the file and aborts non-zero. The bundled config/focus validity leg is automated by TC-061
    - LLR-044 [Implemented] — Shipped demo config + focus database are a valid configuration
      - TC-061 [Verified] — Load the shipped demo/demo_config.toml via Config::from_file and assert it passes Config::validate with exactly one even-dimension output (demo-1280x800, 1280x800, quality_crf 28, enable_audio=true) referencing focus.json; load demo/focus.json via RoiDb::load and assert two entries — the point (cat.jpg) and the bbox-centre (flower.jpg) — resolve. Guards the demo kit so a broken config/focus fails CI not an end user
- SN-030
  - SR-034 [Draft] — Selectable video encoder with hardware probe and software fallback
    - LLR-045 [Draft] — Pure encoder arg-list mapping per encoder choice
      - TC-063 [Draft] — Call the pure encoder_args(choice, crf, preset) mapping across the full SR-034 encoder set: assert `software` yields today's libx264 arg block byte-identically, each hardware choice maps quality_crf to its encoder's quality control (-cq for h264_nvenc, -global_quality for h264_qsv/h264_amf), and the SR-005 profile fields (-pix_fmt yuv420p, -movflags +faststart, -f mp4, -an) are invariant across every choice. No I/O; mirrors the resolve::pick pattern (LLR-032)
      - TC-068 [Draft] — Build with encoder=software explicit and x264_preset at its boundary values (veryfast, slow) plus unset (default medium); ffprobe each output with the existing encode_profile pattern (TC-010) asserting the SR-005 profile: codec=h264, container=mp4, pix_fmt=yuv420p, faststart, even dims, 24<=fps<=30. The software-encoder profile leg of SR-034 and the any-supported-preset profile leg of SR-035
    - LLR-046 [Draft] — Preflight hardware-encoder probe (listing + 1-frame trial encode)
      - TC-064 [Draft] — Feed parse_encoders sample `ffmpeg -encoders` listing text (encoder present vs absent) and the trial-encode classifier sample exit statuses + stderr; assert ProbeOutcome resolves to Pass/Fail(reason)/Absent correctly, and that probe results are cached per (ffmpeg, encoder) so a multi-output run (SR-017) probes each encoder once. Pure parse/classify legs of the preflight probe
      - TC-067 [Draft] — Build (built binary) with encoder=auto on a host where no hardware encoder passes the probe (GPU-less CI runner; on developer boxes force the same path with an explicit hardware encoder whose trial encode fails, e.g. h264_qsv on a non-Intel host); assert the run logs the fallback WARN naming the encoder and probe reason, reports the encoder actually used (libx264 fallback), completes with exit zero, and ffprobe of the output passes the SR-005 profile checks
    - LLR-047 [Draft] — Per-output `encoder` config field with validation
      - TC-066 [Draft] — Deserialize OutputDef TOML with the encoder field omitted (defaults to software so existing configs parse and the SR-005-verified arg list is unchanged), with each valid SR-034 value, and with an invalid value; assert Config::validate rejects the invalid value with a plain-language error naming the field and the allowed set, and that the field is per-output (two [[outputs]] may select different encoders)
    - LLR-048 [Draft] — Fallback decision + encoder-in-use reporting
      - TC-065 [Draft] — Call the pure select_encoder(choice, outcome) over the SR-034 Permutations matrix (encoder x probe, @pairwise): unset/software never consults the probe and yields libx264 unchanged; auto+Pass yields the probed hardware encoder; auto or an explicit hardware choice with Fail/Absent yields software plus a logged fallback reason naming the encoder; assert NO combination returns an error/abort (a missing GPU never fails the build)
      - TC-067 [Draft] — Build (built binary) with encoder=auto on a host where no hardware encoder passes the probe (GPU-less CI runner; on developer boxes force the same path with an explicit hardware encoder whose trial encode fails, e.g. h264_qsv on a non-Intel host); assert the run logs the fallback WARN naming the encoder and probe reason, reports the encoder actually used (libx264 fallback), completes with exit zero, and ffprobe of the output passes the SR-005 profile checks
      - TC-069 [Draft] — Demonstration of the SR-034 hardware leg on NVENC-capable hardware: build with encoder=h264_nvenc, assert the run reports nvenc in use and the output passes the SR-005 ffprobe checks. Automated=No: needs a working GPU encoder, absent on CI runners (same stance as TC-032/TC-040); procedure in Parameters
    - TC-071 [Draft] — Inspect the quick-reference: the encoder and x264_preset config fields are documented with their defaults (software / medium), the allowed value sets, and the preset quality/speed trade stated once (single home per SR-020); the hardware-fallback behavior (a missing GPU never fails the build) is described in plain language
  - SR-035 [Draft] — Software x264 preset knob (default medium unchanged)
    - LLR-049 [Draft] — x264_preset field threaded to software-encoder args
      - TC-068 [Draft] — Build with encoder=software explicit and x264_preset at its boundary values (veryfast, slow) plus unset (default medium); ffprobe each output with the existing encode_profile pattern (TC-010) asserting the SR-005 profile: codec=h264, container=mp4, pix_fmt=yuv420p, faststart, even dims, 24<=fps<=30. The software-encoder profile leg of SR-034 and the any-supported-preset profile leg of SR-035
      - TC-070 [Draft] — Assert the preset pure leg: unset x264_preset yields `-preset medium` byte-identically to today's args; each supported value passes through encoder_args as `-preset <value>` on the software path ONLY (hardware arg lists carry no x264 preset); an unsupported value is rejected by Config::validate with a plain-language error naming the field and allowed values
    - TC-071 [Draft] — Inspect the quick-reference: the encoder and x264_preset config fields are documented with their defaults (software / medium), the allowed value sets, and the preset quality/speed trade stated once (single home per SR-020); the hardware-fallback behavior (a missing GPU never fails the build) is described in plain language
  - SR-036 [Draft] — Per-stage build timings at --verbose and perf-metrics emission
    - LLR-050 [Draft] — Stage-timer instrumentation in the hot path
      - TC-072 [Draft] — Build (built binary) a tiny album with --verbose and capture output: assert one timing line per SR-036 stage (decode, prescale, render, blend, encode-write-stall, ffmpeg-wall) per output, each naming the stage with an elapsed time; then build without --verbose and assert output is unchanged apart from the existing end-of-output frames/s line
    - LLR-051 [Draft] — perf-metrics.json writer keyed by PB-ID
      - TC-073 [Draft] — Call write_perf_metrics with a PB-ID->number map and read the file back: assert valid JSON of shape {PB-ID: number} with one entry per measured performance-budget row (PB-001..PB-005), numeric values, and keys that exist in performance-budgets.csv. Pure serialization leg, separable from measurement
      - TC-074 [Draft] — Release bench procedure (dev box): run Scripts/bench.ps1 (wraps bench --full over the fixed bench corpus, cold then warm); assert docs/test/perf-metrics.json is written with one numeric entry per PB row in performance-budgets.csv — including PB-002 (boundary stall) and PB-005 (peak working set), the regression guards for the no-SR prefetch/overlap restructures (LLR-060/LLR-063) — then run python Scripts/check_perf.py and assert it consumes the file without error, writes perf-report.md, and reports no failing findings (all rows Gate=warn); record the baseline plus named hardware in status.md. Automated=No: hardware-bound wall-clock numbers, same stance as the other Tier=Release TCs
    - LLR-052 [Draft] — Bench-corpus runner emitting metrics
      - TC-074 [Draft] — Release bench procedure (dev box): run Scripts/bench.ps1 (wraps bench --full over the fixed bench corpus, cold then warm); assert docs/test/perf-metrics.json is written with one numeric entry per PB row in performance-budgets.csv — including PB-002 (boundary stall) and PB-005 (peak working set), the regression guards for the no-SR prefetch/overlap restructures (LLR-060/LLR-063) — then run python Scripts/check_perf.py and assert it consumes the file without error, writes perf-report.md, and reports no failing findings (all rows Gate=warn); record the baseline plus named hardware in status.md. Automated=No: hardware-bound wall-clock numbers, same stance as the other Tier=Release TCs
    - LLR-060 [Draft] — Background clip prefetch with bounded lookahead
      - TC-086 [Draft] — With background clip prefetch active (bounded lookahead 1-2), build over an album containing a corrupt clip among valid ones: assert the failed prefetch Result is delivered IN ORDER to the clip loop and routed through record_skip so the skipped count, per-skip path+reason, and exit semantics are identical to the serial path; the existing SR-014 suites (TC-022 skip-and-continue, TC-023 some-bad/all-bad exits) must stay green unchanged — referenced, not duplicated. Boundary-stall reduction is the PB-002 measurement via TC-074's bench run, not asserted here
    - LLR-061 [Draft] — SIMD resize fast path when rotation is off
      - TC-087 [Draft] — Render the same path-seeded clip with max_rotation_degrees=0 (SIMD resize fast path) and >0 (warp_into rotation path): assert the fast path is selected iff rotation is off, both paths emit frames of the exact expected byte size, and the ClipPlan motion parameters are identical for the same seed (SR-022 determinism is plan-level; bit-exactness vs the generic resampler is NOT claimed). Existing determinism suites TC-029/TC-030/TC-052 stay green unchanged. Render-timer movement is the PB-001 evidence via TC-074
    - LLR-062 [Draft] — Fixed-point blend/scale + surplus-head clone removal
      - TC-088 [Draft] — Compare the u16 fixed-point blend/scale ((a*(256-w) + b*w + 128) >> 8) against the f32 reference over boundary and interior weights and pixel values: assert every channel of every pixel is within +-1 LSB, the zero weight returns the first frame exactly and the full weight returns the second exactly; assert the surplus-head clone in add_clip is gone (frames consumed by value). SR-022 motion determinism is untouched (weights quantize, motion parameters do not)
    - LLR-063 [Draft] — Encoder-writer thread + bounded channels replace batch memory budget
      - TC-089 [Draft] — Build the same album through the overlapped stage graph (prefetch -> rayon render pool -> mixer -> EncoderWriter thread over bounded sync_channels) and through the serial reference; assert the outputs match (frame count, duration, SR-005 profile via ffprobe) and that channel depth (~2x fade_frames) bounds memory in place of the deleted RANGE_MEMORY_BUDGET (peak working set is the PB-005 measurement via TC-074). The existing behavioral suites must stay green unchanged — TC-018 (atomic finalize), TC-021 (watchdog), TC-022/TC-023 (skip), TC-057/TC-058/TC-059 (audio) — referenced, not duplicated
  - SR-038 [Draft] — Media-scan probe cache keyed by file identity
    - LLR-058 [Draft] — Probe-cache JSON persistence in per-user cache dir
      - TC-084 [Draft] — Drive ProbeCache persistence: a missing cache file starts empty (cold behavior, scan never fails); a corrupt/truncated cache file is discarded and the scan proceeds cold; save rewrites atomically (tmp + rename) so a killed save never corrupts the cache; the file lands under the per-user cache dir keyed by a hash of media_root (never under media_root per SR-010, never beside outputs)
      - TC-085 [Draft] — Scan a temp library cold then warm across the SR-038 entry x kind Permutations (@pairwise): hit-unchanged runs ZERO probe subprocesses/header decodes for cached entries (asserted via probe-invocation counting/logging) and the warm scan result equals the cold scan (same items, kinds, dims, durations, has_audio); miss-new (a new file) is probed and its entry cached; changed-size and changed-mtime (a resized/touched file) are re-probed and the entry replaced; corrupt-entry is discarded and re-probed without failing the scan. Skip and ignore semantics identical warm and cold — guarded by the existing TC-022/TC-023 (SR-014) and TC-028 (SR-018) suites, referenced not duplicated
    - LLR-059 [Draft] — Scan integration: keyed lookup vs re-probe decision
      - TC-083 [Draft] — Call the pure entry_current(entry, size, mtime) across the match matrix: both-match accepts the cached entry (hit); size-differs, mtime-differs, and both-differ each reject (re-probe). The hit/re-probe decision leg of the scan cache
      - TC-085 [Draft] — Scan a temp library cold then warm across the SR-038 entry x kind Permutations (@pairwise): hit-unchanged runs ZERO probe subprocesses/header decodes for cached entries (asserted via probe-invocation counting/logging) and the warm scan result equals the cold scan (same items, kinds, dims, durations, has_audio); miss-new (a new file) is probed and its entry cached; changed-size and changed-mtime (a resized/touched file) are re-probed and the entry replaced; corrupt-entry is discarded and re-probed without failing the scan. Skip and ignore semantics identical warm and cold — guarded by the existing TC-022/TC-023 (SR-014) and TC-028 (SR-018) suites, referenced not duplicated
- SN-031
  - SR-036 [Draft] — Per-stage build timings at --verbose and perf-metrics emission
    - LLR-050 [Draft] — Stage-timer instrumentation in the hot path
      - TC-072 [Draft] — Build (built binary) a tiny album with --verbose and capture output: assert one timing line per SR-036 stage (decode, prescale, render, blend, encode-write-stall, ffmpeg-wall) per output, each naming the stage with an elapsed time; then build without --verbose and assert output is unchanged apart from the existing end-of-output frames/s line
    - LLR-051 [Draft] — perf-metrics.json writer keyed by PB-ID
      - TC-073 [Draft] — Call write_perf_metrics with a PB-ID->number map and read the file back: assert valid JSON of shape {PB-ID: number} with one entry per measured performance-budget row (PB-001..PB-005), numeric values, and keys that exist in performance-budgets.csv. Pure serialization leg, separable from measurement
      - TC-074 [Draft] — Release bench procedure (dev box): run Scripts/bench.ps1 (wraps bench --full over the fixed bench corpus, cold then warm); assert docs/test/perf-metrics.json is written with one numeric entry per PB row in performance-budgets.csv — including PB-002 (boundary stall) and PB-005 (peak working set), the regression guards for the no-SR prefetch/overlap restructures (LLR-060/LLR-063) — then run python Scripts/check_perf.py and assert it consumes the file without error, writes perf-report.md, and reports no failing findings (all rows Gate=warn); record the baseline plus named hardware in status.md. Automated=No: hardware-bound wall-clock numbers, same stance as the other Tier=Release TCs
    - LLR-052 [Draft] — Bench-corpus runner emitting metrics
      - TC-074 [Draft] — Release bench procedure (dev box): run Scripts/bench.ps1 (wraps bench --full over the fixed bench corpus, cold then warm); assert docs/test/perf-metrics.json is written with one numeric entry per PB row in performance-budgets.csv — including PB-002 (boundary stall) and PB-005 (peak working set), the regression guards for the no-SR prefetch/overlap restructures (LLR-060/LLR-063) — then run python Scripts/check_perf.py and assert it consumes the file without error, writes perf-report.md, and reports no failing findings (all rows Gate=warn); record the baseline plus named hardware in status.md. Automated=No: hardware-bound wall-clock numbers, same stance as the other Tier=Release TCs
    - LLR-060 [Draft] — Background clip prefetch with bounded lookahead
      - TC-086 [Draft] — With background clip prefetch active (bounded lookahead 1-2), build over an album containing a corrupt clip among valid ones: assert the failed prefetch Result is delivered IN ORDER to the clip loop and routed through record_skip so the skipped count, per-skip path+reason, and exit semantics are identical to the serial path; the existing SR-014 suites (TC-022 skip-and-continue, TC-023 some-bad/all-bad exits) must stay green unchanged — referenced, not duplicated. Boundary-stall reduction is the PB-002 measurement via TC-074's bench run, not asserted here
    - LLR-061 [Draft] — SIMD resize fast path when rotation is off
      - TC-087 [Draft] — Render the same path-seeded clip with max_rotation_degrees=0 (SIMD resize fast path) and >0 (warp_into rotation path): assert the fast path is selected iff rotation is off, both paths emit frames of the exact expected byte size, and the ClipPlan motion parameters are identical for the same seed (SR-022 determinism is plan-level; bit-exactness vs the generic resampler is NOT claimed). Existing determinism suites TC-029/TC-030/TC-052 stay green unchanged. Render-timer movement is the PB-001 evidence via TC-074
    - LLR-062 [Draft] — Fixed-point blend/scale + surplus-head clone removal
      - TC-088 [Draft] — Compare the u16 fixed-point blend/scale ((a*(256-w) + b*w + 128) >> 8) against the f32 reference over boundary and interior weights and pixel values: assert every channel of every pixel is within +-1 LSB, the zero weight returns the first frame exactly and the full weight returns the second exactly; assert the surplus-head clone in add_clip is gone (frames consumed by value). SR-022 motion determinism is untouched (weights quantize, motion parameters do not)
    - LLR-063 [Draft] — Encoder-writer thread + bounded channels replace batch memory budget
      - TC-089 [Draft] — Build the same album through the overlapped stage graph (prefetch -> rayon render pool -> mixer -> EncoderWriter thread over bounded sync_channels) and through the serial reference; assert the outputs match (frame count, duration, SR-005 profile via ffprobe) and that channel depth (~2x fade_frames) bounds memory in place of the deleted RANGE_MEMORY_BUDGET (peak working set is the PB-005 measurement via TC-074). The existing behavioral suites must stay green unchanged — TC-018 (atomic finalize), TC-021 (watchdog), TC-022/TC-023 (skip), TC-057/TC-058/TC-059 (audio) — referenced, not duplicated
  - SR-037 [Draft] — Incremental per-clip segment cache for warm rebuilds
    - LLR-053 [Draft] — Pure segment cache-key derivation
      - TC-075 [Draft] — Call the pure segment_key over a baseline SegmentKeyInput and single-field variations: assert identical input yields an identical key and that changing ANY key-tuple member — source path/size/mtime, each encode-relevant OutputDef field (dims, fps, quality_crf, fade/pic times, ken_burns, zoom, rotation, encoder, preset), left or right neighbor identity (including edge None vs Some), engine version — yields a different key, bounding invalidation to inserted items plus their two neighbors
      - TC-081 [Draft] — Drive build over a temp album through the SR-037 scenario x cache-flag Permutations (@pairwise): cold populates the cache; warm-unchanged re-encodes 0 segments, asserted honestly via the cache hit/miss counts surfaced in the summary/log (only misses re-encoded); added-photos re-encodes only the new clips plus transition neighbors; removed-photo only the neighbors; changed-settings (resolution, quality, encoder, preset) invalidates the affected output's segments — a full/partial miss re-encoded, never a stale segment served; corrupted-cache-entry is re-encoded and the build succeeds; every warm output is equivalent to its cold build (same frame count and duration, SR-005 profile via ffprobe). Atomic-finalize/failure legs are TC-080
    - LLR-054 [Draft] — Segment store layout + LRU prune + cache flags
      - TC-078 [Draft] — Drive SegmentStore directly: insert/lookup round-trips a segment; prune_to(size_cap) evicts least-recently-used entries down to the cap; a corrupt/unreadable segment file or index is treated as a miss and deleted without failing the caller; --no-cache neither reads nor writes the store during a build; --clear-cache empties the store then proceeds; the store roots at ProcessingConfig.temp_dir when set else the per-user cache dir
      - TC-081 [Draft] — Drive build over a temp album through the SR-037 scenario x cache-flag Permutations (@pairwise): cold populates the cache; warm-unchanged re-encodes 0 segments, asserted honestly via the cache hit/miss counts surfaced in the summary/log (only misses re-encoded); added-photos re-encodes only the new clips plus transition neighbors; removed-photo only the neighbors; changed-settings (resolution, quality, encoder, preset) invalidates the affected output's segments — a full/partial miss re-encoded, never a stale segment served; corrupted-cache-entry is re-encoded and the build succeeds; every warm output is equivalent to its cold build (same frame count and duration, SR-005 profile via ffprobe). Atomic-finalize/failure legs are TC-080
    - LLR-055 [Draft] — Warm-build segment planner (hit/miss partition)
      - TC-076 [Draft] — Call the pure plan_segments over an ordered album and a store key set per scenario: warm-unchanged partitions all segments as hits (0 misses); added-photos marks only the new clips plus their transition neighbors as misses; removed-photo marks only the removed clip's neighbors; assert each segment descriptor spans second-half-transition-in + body + first-half-transition-out and that per-segment output start frames and frame counts sum to the album total minus transition overlaps
      - TC-081 [Draft] — Drive build over a temp album through the SR-037 scenario x cache-flag Permutations (@pairwise): cold populates the cache; warm-unchanged re-encodes 0 segments, asserted honestly via the cache hit/miss counts surfaced in the summary/log (only misses re-encoded); added-photos re-encodes only the new clips plus transition neighbors; removed-photo only the neighbors; changed-settings (resolution, quality, encoder, preset) invalidates the affected output's segments — a full/partial miss re-encoded, never a stale segment served; corrupted-cache-entry is re-encoded and the build succeeds; every warm output is equivalent to its cold build (same frame count and duration, SR-005 profile via ffprobe). Atomic-finalize/failure legs are TC-080
    - LLR-056 [Draft] — Concat assembly routed through watchdog + disk-full + atomic promote
      - TC-079 [Draft] — Concat-seam spike (plan §4 mandatory early risk burn-down; first Round 3 item): build the same small fixed corpus twice with the software encoder — (a) cold/streaming, (b) segmented + `ffmpeg -f concat -c copy` assembly; ffprobe both outputs and assert identical total frame count, identical duration, and the SR-005 profile on the concat output; decode a window around every segment join and assert no dropped/duplicated frames (frame-count + per-frame hash comparison across each join). CI-runnable with the software encoder; the real-frame visual leg is TC-082
      - TC-080 [Draft] — Force failures in the concat assembly path: a non-zero concat exit surfaces as a named SlideshowError with non-zero exit and no false success; the concat child is armed with the same ffmpeg_timeout_secs watchdog (inactivity aborts, naming the output); a disk-full write maps through is_disk_full/disk_full_error naming the path; killing mid-concat leaves NO final <name>.mp4 — the final name appears only via promote() and the .part is cleaned
      - TC-082 [Draft] — Demonstration on the dev box plus a real target picture frame (plan §4 gate; scheduled BEFORE any cache implementation commit per the R2 MINOR): build the bench corpus segmented+concat with software and h264_nvenc encoders, play both outputs on the physical frame, and visually confirm seam-free playback — no stutter, flash, or timestamp jump at any segment join. Automated=No: target-hardware visual judgment
    - LLR-057 [Draft] — Audio-delay bookkeeping across the concat timeline
      - TC-077 [Draft] — For an album mixing images and audio-bearing video, compute each audio clip's output start frame from SegmentPlan frame offsets (cached path) and from the streaming mixer.emitted() capture (LLR-040 path); assert the two agree for every clip so warm and cold builds mux identical adelay values through the unchanged delay_ms (TC-055 math)
  - SR-038 [Draft] — Media-scan probe cache keyed by file identity
    - LLR-058 [Draft] — Probe-cache JSON persistence in per-user cache dir
      - TC-084 [Draft] — Drive ProbeCache persistence: a missing cache file starts empty (cold behavior, scan never fails); a corrupt/truncated cache file is discarded and the scan proceeds cold; save rewrites atomically (tmp + rename) so a killed save never corrupts the cache; the file lands under the per-user cache dir keyed by a hash of media_root (never under media_root per SR-010, never beside outputs)
      - TC-085 [Draft] — Scan a temp library cold then warm across the SR-038 entry x kind Permutations (@pairwise): hit-unchanged runs ZERO probe subprocesses/header decodes for cached entries (asserted via probe-invocation counting/logging) and the warm scan result equals the cold scan (same items, kinds, dims, durations, has_audio); miss-new (a new file) is probed and its entry cached; changed-size and changed-mtime (a resized/touched file) are re-probed and the entry replaced; corrupt-entry is discarded and re-probed without failing the scan. Skip and ignore semantics identical warm and cold — guarded by the existing TC-022/TC-023 (SR-014) and TC-028 (SR-018) suites, referenced not duplicated
    - LLR-059 [Draft] — Scan integration: keyed lookup vs re-probe decision
      - TC-083 [Draft] — Call the pure entry_current(entry, size, mtime) across the match matrix: both-match accepts the cached entry (hit); size-differs, mtime-differs, and both-differ each reject (re-probe). The hit/re-probe decision leg of the scan cache
      - TC-085 [Draft] — Scan a temp library cold then warm across the SR-038 entry x kind Permutations (@pairwise): hit-unchanged runs ZERO probe subprocesses/header decodes for cached entries (asserted via probe-invocation counting/logging) and the warm scan result equals the cold scan (same items, kinds, dims, durations, has_audio); miss-new (a new file) is probed and its entry cached; changed-size and changed-mtime (a resized/touched file) are re-probed and the entry replaced; corrupt-entry is discarded and re-probed without failing the scan. Skip and ignore semantics identical warm and cold — guarded by the existing TC-022/TC-023 (SR-014) and TC-028 (SR-018) suites, referenced not duplicated

## Traceability graph

_The chain as a DAG, colored by state (orphan/draft stand out). Small and diff-friendly; run `--html` for the scalable full-graph view._

```mermaid
graph LR
    classDef orphan fill:#ffd6d6,stroke:#cc0000,color:#000;
    classDef draft fill:#fff3cd,stroke:#cc9900,color:#000;
    SN_001["SN-001"]
    SN_002["SN-002"]
    SN_003["SN-003"]
    SN_004["SN-004"]
    SN_005["SN-005"]
    SN_006["SN-006"]
    SN_007["SN-007"]
    SN_008["SN-008"]
    SN_009["SN-009"]
    SN_010["SN-010"]
    SN_011["SN-011"]
    SN_012["SN-012"]
    SN_013["SN-013"]
    SN_014["SN-014"]
    SN_015["SN-015"]
    SN_016["SN-016"]
    SN_017["SN-017"]
    SN_018["SN-018"]
    SN_019["SN-019"]
    SN_020["SN-020"]
    SN_021["SN-021"]
    SN_022["SN-022"]
    SN_023["SN-023"]
    SN_024["SN-024"]
    SN_025["SN-025"]
    SN_026["SN-026"]
    SN_027["SN-027"]
    SN_028["SN-028"]
    SN_029["SN-029"]
    SN_030["SN-030"]
    SN_031["SN-031"]
    SR_001["SR-001 — End-user download-to-first-slideshow pa…"]
    SR_002["SR-002 — Pre-run validate gates build on runtime…"]
    SR_003["SR-003 — Single documented config file with defa…"]
    SR_004["SR-004 — Build progress and completion summary"]
    SR_005["SR-005 — Frame-compatible default encode profile"]
    SR_006["SR-006 — Even-dimension enforcement for any conf…"]
    SR_007["SR-007 — Frame-rate bounds"]
    SR_008["SR-008 — Quality/bitrate via CRF default"]
    SR_009["SR-009 — Pre-run oversize (FAT32 4GB) size estim…"]
    SR_010["SR-010 — Source media never modified"]
    SR_011["SR-011 — Crash-safe output (no complete-looking …"]
    SR_012["SR-012 — FFmpeg-missing detection"]
    SR_013["SR-013 — FFmpeg-error handling with no false suc…"]
    SR_014["SR-014 — Corrupt/unsupported input skip-and-cont…"]
    SR_015["SR-015 — Storage-full detection without complete…"]
    SR_016["SR-016 — Output/input path missing or unwritable…"]
    SR_017["SR-017 — Multiple output definitions per run"]
    SR_018["SR-018 — Case-insensitive ignore list"]
    SR_019["SR-019 — Honest capability/gap reporting"]
    SR_020["SR-020 — Single discoverable quick-reference"]
    SR_021["SR-021 — No conflicting restated constraints acr…"]
    SR_022["SR-022 — Deterministic reproducible output"]
    SR_023["SR-023 — Prebuilt binary distribution via CI to …"]
    SR_024["SR-024 — In-binary startup dependency self-check…"]
    SR_025["SR-025 — End User vs Developer documentation aud…"]
    SR_026["SR-026 — First-run GUI wizard collects settings …"]
    SR_027["SR-027 — Auto-fetch FFmpeg to per-user dir with …"]
    SR_028["SR-028 — Interactive-only prompts; non-interacti…"]
    SR_029["SR-029 — Downloaded FFmpeg integrity-verified ag…"]
    SR_030["SR-030 — Config-write location precedence report…"]
    SR_031["SR-031 — Sub-pixel-smooth Ken Burns with optiona…"]
    SR_032["SR-032 — Source-video audio passthrough synced t…"]
    SR_033["SR-033 — Bundled one-click demo (sample media + …"]
    SR_034["SR-034 — Selectable video encoder with hardware …"]
    SR_035["SR-035 — Software x264 preset knob (default medi…"]
    SR_036["SR-036 — Per-stage build timings at --verbose an…"]
    SR_037["SR-037 — Incremental per-clip segment cache for …"]
    SR_038["SR-038 — Media-scan probe cache keyed by file id…"]
    LLR_001["LLR-001 — Single-file TOML/JSON config load with …"]
    LLR_002["LLR-002 — Config struct field set with documented…"]
    LLR_003["LLR-003 — Config semantic validation"]
    LLR_004["LLR-004 — Runtime-prerequisite checks with per-it…"]
    LLR_005["LLR-005 — Build refuses to start when essential p…"]
    LLR_006["LLR-006 — FFmpeg-missing detection with named pla…"]
    LLR_007["LLR-007 — FFmpeg non-zero exit surfaced as error,…"]
    LLR_008["LLR-008 — Encode failure names affected output an…"]
    LLR_009["LLR-009 — Frame-compatible H.264/MP4 encode profi…"]
    LLR_010["LLR-010 — Even-dimension enforcement for any conf…"]
    LLR_011["LLR-011 — Configurable frame rate honored and def…"]
    LLR_012["LLR-012 — Quality via CRF passthrough"]
    LLR_013["LLR-013 — Pre-run/post-run oversize estimate and …"]
    LLR_014["LLR-014 — Source media opened strictly read-only"]
    LLR_015["LLR-015 — Atomic temp-to-final output finalize"]
    LLR_016["LLR-016 — Skip-and-continue on unreadable/unsuppo…"]
    LLR_017["LLR-017 — Skipped-count and per-skip reason in co…"]
    LLR_018["LLR-018 — Disk-full write failure detected and na…"]
    LLR_019["LLR-019 — Output/input path missing-or-unwritable…"]
    LLR_020["LLR-020 — Multiple output definitions in one build"]
    LLR_021["LLR-021 — Case-insensitive ignore-pattern exclusi…"]
    LLR_022["LLR-022 — Deterministic path-seeded Ken Burns mot…"]
    LLR_023["LLR-023 — Per-stage progress reporting during bui…"]
    LLR_024["LLR-024 — Completion summary of written outputs (…"]
    LLR_025["LLR-025 — CI-built make_video_slideshow.exe publi…"]
    LLR_026["LLR-026 — In-binary startup self-check + first-ru…"]
    LLR_027["LLR-027 — End-user download-to-first-slideshow pa…"]
    LLR_028["LLR-028 — Documentation-realized SRs (capability …"]
    LLR_029["LLR-029 — First-run GUI wizard (PowerShell/WinFor…"]
    LLR_030["LLR-030 — Pure wizard-values→valid Config/TOML ma…"]
    LLR_031["LLR-031 — FFmpeg auto-fetch to per-user dir with …"]
    LLR_032["LLR-032 — FFmpeg resolution-order helper (PATH/co…"]
    LLR_033["LLR-033 — Interactive vs non-interactive gating (…"]
    LLR_034["LLR-034 — Fetched-FFmpeg integrity verification a…"]
    LLR_035["LLR-035 — Config-write location resolver (beside-…"]
    LLR_036["LLR-036 — Sub-pixel single-warp Ken Burns render …"]
    LLR_037["LLR-037 — Fixed-focus anchoring (per-output defau…"]
    LLR_038["LLR-038 — Region-of-interest JSON database load +…"]
    LLR_039["LLR-039 — Per-file audio-stream probe (has_audio)"]
    LLR_040["LLR-040 — Per-clip output-start capture and audio…"]
    LLR_041["LLR-041 — Audio mux pass (adelay/amix/AAC) with a…"]
    LLR_043["LLR-043 — demo.bat + bundled demo config/focus + …"]
    LLR_044["LLR-044 — Shipped demo config + focus database ar…"]
    LLR_042["LLR-042 — Per-output audio config fields"]
    LLR_045["LLR-045 — Pure encoder arg-list mapping per encod…"]
    LLR_046["LLR-046 — Preflight hardware-encoder probe (listi…"]
    LLR_047["LLR-047 — Per-output `encoder` config field with …"]
    LLR_048["LLR-048 — Fallback decision + encoder-in-use repo…"]
    LLR_049["LLR-049 — x264_preset field threaded to software-…"]
    LLR_050["LLR-050 — Stage-timer instrumentation in the hot …"]
    LLR_051["LLR-051 — perf-metrics.json writer keyed by PB-ID"]
    LLR_052["LLR-052 — Bench-corpus runner emitting metrics"]
    LLR_053["LLR-053 — Pure segment cache-key derivation"]
    LLR_054["LLR-054 — Segment store layout + LRU prune + cach…"]
    LLR_055["LLR-055 — Warm-build segment planner (hit/miss pa…"]
    LLR_056["LLR-056 — Concat assembly routed through watchdog…"]
    LLR_057["LLR-057 — Audio-delay bookkeeping across the conc…"]
    LLR_058["LLR-058 — Probe-cache JSON persistence in per-use…"]
    LLR_059["LLR-059 — Scan integration: keyed lookup vs re-pr…"]
    LLR_060["LLR-060 — Background clip prefetch with bounded l…"]
    LLR_061["LLR-061 — SIMD resize fast path when rotation is …"]
    LLR_062["LLR-062 — Fixed-point blend/scale + surplus-head …"]
    LLR_063["LLR-063 — Encoder-writer thread + bounded channel…"]
    LLR_064["LLR-064 — Cross-thread failure propagation preser…"]
    TC_001["TC-001"]
    TC_002["TC-002"]
    TC_003["TC-003"]
    TC_004["TC-004"]
    TC_005["TC-005"]
    TC_006["TC-006"]
    TC_007["TC-007"]
    TC_008["TC-008"]
    TC_009["TC-009"]
    TC_010["TC-010"]
    TC_011["TC-011"]
    TC_012["TC-012"]
    TC_013["TC-013"]
    TC_014["TC-014"]
    TC_015["TC-015"]
    TC_016["TC-016"]
    TC_017["TC-017"]
    TC_018["TC-018"]
    TC_019["TC-019"]
    TC_020["TC-020"]
    TC_021["TC-021"]
    TC_022["TC-022"]
    TC_023["TC-023"]
    TC_024["TC-024"]
    TC_025["TC-025"]
    TC_026["TC-026"]
    TC_027["TC-027"]
    TC_028["TC-028"]
    TC_029["TC-029"]
    TC_030["TC-030"]
    TC_031["TC-031"]
    TC_032["TC-032"]
    TC_033["TC-033"]
    TC_034["TC-034"]
    TC_035["TC-035"]
    TC_036["TC-036"]
    TC_037["TC-037"]
    TC_038["TC-038"]
    TC_039["TC-039"]
    TC_040["TC-040"]
    TC_041["TC-041"]
    TC_042["TC-042"]
    TC_043["TC-043"]
    TC_044["TC-044"]
    TC_045["TC-045"]
    TC_046["TC-046"]
    TC_047["TC-047"]
    TC_048["TC-048"]
    TC_049["TC-049"]
    TC_050["TC-050"]
    TC_051["TC-051"]
    TC_052["TC-052"]
    TC_053["TC-053"]
    TC_054["TC-054"]
    TC_055["TC-055"]
    TC_056["TC-056"]
    TC_057["TC-057"]
    TC_058["TC-058"]
    TC_059["TC-059"]
    TC_060["TC-060"]
    TC_061["TC-061"]
    TC_062["TC-062"]
    TC_063["TC-063"]
    TC_064["TC-064"]
    TC_065["TC-065"]
    TC_066["TC-066"]
    TC_067["TC-067"]
    TC_068["TC-068"]
    TC_069["TC-069"]
    TC_070["TC-070"]
    TC_071["TC-071"]
    TC_072["TC-072"]
    TC_073["TC-073"]
    TC_074["TC-074"]
    TC_075["TC-075"]
    TC_076["TC-076"]
    TC_077["TC-077"]
    TC_078["TC-078"]
    TC_079["TC-079"]
    TC_080["TC-080"]
    TC_081["TC-081"]
    TC_082["TC-082"]
    TC_083["TC-083"]
    TC_084["TC-084"]
    TC_085["TC-085"]
    TC_086["TC-086"]
    TC_087["TC-087"]
    TC_088["TC-088"]
    TC_089["TC-089"]
    TC_090["TC-090"]
    LLR_001 --> TC_005
    LLR_002 --> TC_006
    LLR_003 --> TC_005
    LLR_003 --> TC_007
    LLR_004 --> TC_002
    LLR_004 --> TC_003
    LLR_004 --> TC_049
    LLR_005 --> TC_004
    LLR_005 --> TC_049
    LLR_006 --> TC_019
    LLR_007 --> TC_020
    LLR_008 --> TC_021
    LLR_009 --> TC_010
    LLR_009 --> TC_014
    LLR_010 --> TC_011
    LLR_010 --> TC_031
    LLR_010 --> TC_046
    LLR_011 --> TC_012
    LLR_011 --> TC_013
    LLR_012 --> TC_014
    LLR_013 --> TC_015
    LLR_013 --> TC_016
    LLR_013 --> TC_047
    LLR_014 --> TC_017
    LLR_015 --> TC_018
    LLR_015 --> TC_025
    LLR_016 --> TC_022
    LLR_017 --> TC_023
    LLR_018 --> TC_024
    LLR_018 --> TC_048
    LLR_019 --> TC_003
    LLR_019 --> TC_026
    LLR_020 --> TC_027
    LLR_021 --> TC_028
    LLR_022 --> TC_029
    LLR_022 --> TC_030
    LLR_023 --> TC_008
    LLR_024 --> TC_009
    LLR_025 --> TC_032
    LLR_026 --> TC_033
    LLR_027 --> TC_001
    LLR_028 --> TC_034
    LLR_028 --> TC_035
    LLR_028 --> TC_036
    LLR_028 --> TC_037
    LLR_029 --> TC_038
    LLR_030 --> TC_039
    LLR_031 --> TC_040
    LLR_032 --> TC_041
    LLR_033 --> TC_042
    LLR_033 --> TC_043
    LLR_034 --> TC_044
    LLR_035 --> TC_045
    LLR_036 --> TC_052
    LLR_037 --> TC_053
    LLR_038 --> TC_054
    LLR_039 --> TC_057
    LLR_039 --> TC_059
    LLR_040 --> TC_055
    LLR_041 --> TC_057
    LLR_041 --> TC_058
    LLR_042 --> TC_056
    LLR_043 --> TC_060
    LLR_044 --> TC_061
    LLR_045 --> TC_063
    LLR_045 --> TC_068
    LLR_046 --> TC_064
    LLR_046 --> TC_067
    LLR_047 --> TC_066
    LLR_048 --> TC_065
    LLR_048 --> TC_067
    LLR_048 --> TC_069
    LLR_049 --> TC_068
    LLR_049 --> TC_070
    LLR_050 --> TC_072
    LLR_051 --> TC_073
    LLR_051 --> TC_074
    LLR_052 --> TC_074
    LLR_053 --> TC_075
    LLR_053 --> TC_081
    LLR_054 --> TC_078
    LLR_054 --> TC_081
    LLR_055 --> TC_076
    LLR_055 --> TC_081
    LLR_056 --> TC_079
    LLR_056 --> TC_080
    LLR_056 --> TC_082
    LLR_057 --> TC_077
    LLR_058 --> TC_084
    LLR_058 --> TC_085
    LLR_059 --> TC_083
    LLR_059 --> TC_085
    LLR_060 --> TC_086
    LLR_061 --> TC_087
    LLR_062 --> TC_088
    LLR_063 --> TC_089
    LLR_064 --> TC_090
    SN_001 --> SR_001
    SN_002 --> SR_002
    SN_003 --> SR_003
    SN_004 --> SR_004
    SN_005 --> SR_005
    SN_005 --> SR_006
    SN_005 --> SR_007
    SN_005 --> SR_008
    SN_005 --> SR_031
    SN_006 --> SR_009
    SN_007 --> SR_006
    SN_007 --> SR_017
    SN_008 --> SR_018
    SN_009 --> SR_019
    SN_009 --> SR_032
    SN_010 --> SR_020
    SN_011 --> SR_020
    SN_011 --> SR_021
    SN_012 --> SR_022
    SN_012 --> SR_031
    SN_013 --> SR_010
    SN_013 --> SR_011
    SN_014 --> SR_012
    SN_014 --> SR_013
    SN_015 --> SR_014
    SN_016 --> SR_011
    SN_016 --> SR_015
    SN_017 --> SR_016
    SN_018 --> SR_009
    SN_019 --> SR_001
    SN_019 --> SR_023
    SN_020 --> SR_001
    SN_020 --> SR_024
    SN_021 --> SR_001
    SN_021 --> SR_025
    SN_022 --> SR_026
    SN_023 --> SR_027
    SN_024 --> SR_028
    SN_025 --> SR_030
    SN_026 --> SR_029
    SN_027 --> SR_031
    SN_028 --> SR_032
    SN_029 --> SR_033
    SN_030 --> SR_034
    SN_030 --> SR_035
    SN_030 --> SR_036
    SN_030 --> SR_038
    SN_031 --> SR_036
    SN_031 --> SR_037
    SN_031 --> SR_038
    SR_001 --> LLR_027
    SR_001 --> TC_001
    SR_002 --> LLR_003
    SR_002 --> LLR_004
    SR_002 --> LLR_005
    SR_002 --> TC_002
    SR_002 --> TC_003
    SR_002 --> TC_004
    SR_002 --> TC_049
    SR_003 --> LLR_001
    SR_003 --> LLR_002
    SR_003 --> LLR_003
    SR_003 --> LLR_044
    SR_003 --> TC_005
    SR_003 --> TC_006
    SR_003 --> TC_007
    SR_003 --> TC_061
    SR_003 --> TC_062
    SR_004 --> LLR_023
    SR_004 --> LLR_024
    SR_004 --> TC_008
    SR_004 --> TC_009
    SR_004 --> TC_050
    SR_004 --> TC_051
    SR_005 --> LLR_009
    SR_005 --> LLR_010
    SR_005 --> TC_010
    SR_005 --> TC_011
    SR_005 --> TC_050
    SR_006 --> LLR_010
    SR_006 --> TC_011
    SR_006 --> TC_031
    SR_006 --> TC_046
    SR_007 --> LLR_011
    SR_007 --> TC_012
    SR_007 --> TC_013
    SR_008 --> LLR_009
    SR_008 --> LLR_012
    SR_008 --> TC_014
    SR_009 --> LLR_013
    SR_009 --> LLR_024
    SR_009 --> TC_009
    SR_009 --> TC_015
    SR_009 --> TC_016
    SR_009 --> TC_047
    SR_010 --> LLR_014
    SR_010 --> TC_017
    SR_011 --> LLR_015
    SR_011 --> LLR_056
    SR_011 --> LLR_063
    SR_011 --> LLR_064
    SR_011 --> TC_018
    SR_011 --> TC_080
    SR_011 --> TC_089
    SR_011 --> TC_090
    SR_012 --> LLR_005
    SR_012 --> LLR_006
    SR_012 --> TC_019
    SR_013 --> LLR_007
    SR_013 --> LLR_008
    SR_013 --> LLR_056
    SR_013 --> LLR_063
    SR_013 --> LLR_064
    SR_013 --> TC_020
    SR_013 --> TC_021
    SR_013 --> TC_080
    SR_013 --> TC_089
    SR_013 --> TC_090
    SR_014 --> LLR_016
    SR_014 --> LLR_017
    SR_014 --> LLR_059
    SR_014 --> LLR_060
    SR_014 --> TC_022
    SR_014 --> TC_023
    SR_014 --> TC_086
    SR_015 --> LLR_015
    SR_015 --> LLR_018
    SR_015 --> LLR_056
    SR_015 --> LLR_063
    SR_015 --> LLR_064
    SR_015 --> TC_024
    SR_015 --> TC_025
    SR_015 --> TC_048
    SR_015 --> TC_080
    SR_015 --> TC_089
    SR_015 --> TC_090
    SR_016 --> LLR_019
    SR_016 --> TC_003
    SR_016 --> TC_026
    SR_017 --> LLR_020
    SR_017 --> TC_027
    SR_017 --> TC_050
    SR_018 --> LLR_021
    SR_018 --> LLR_059
    SR_018 --> TC_028
    SR_019 --> LLR_028
    SR_019 --> TC_034
    SR_020 --> LLR_028
    SR_020 --> TC_035
    SR_021 --> LLR_028
    SR_021 --> TC_036
    SR_022 --> LLR_022
    SR_022 --> LLR_061
    SR_022 --> LLR_062
    SR_022 --> TC_029
    SR_022 --> TC_030
    SR_022 --> TC_087
    SR_022 --> TC_088
    SR_023 --> LLR_025
    SR_023 --> TC_032
    SR_024 --> LLR_026
    SR_024 --> TC_033
    SR_025 --> LLR_028
    SR_025 --> TC_037
    SR_026 --> LLR_029
    SR_026 --> LLR_030
    SR_026 --> TC_038
    SR_026 --> TC_039
    SR_027 --> LLR_031
    SR_027 --> LLR_032
    SR_027 --> TC_040
    SR_027 --> TC_041
    SR_028 --> LLR_033
    SR_028 --> TC_042
    SR_028 --> TC_043
    SR_029 --> LLR_034
    SR_029 --> TC_044
    SR_030 --> LLR_035
    SR_030 --> TC_045
    SR_031 --> LLR_036
    SR_031 --> LLR_037
    SR_031 --> LLR_038
    SR_031 --> LLR_044
    SR_031 --> TC_052
    SR_031 --> TC_053
    SR_031 --> TC_054
    SR_031 --> TC_061
    SR_032 --> LLR_039
    SR_032 --> LLR_040
    SR_032 --> LLR_041
    SR_032 --> LLR_042
    SR_032 --> LLR_057
    SR_032 --> TC_055
    SR_032 --> TC_056
    SR_032 --> TC_057
    SR_032 --> TC_058
    SR_032 --> TC_059
    SR_032 --> TC_077
    SR_033 --> LLR_043
    SR_033 --> LLR_044
    SR_033 --> TC_060
    SR_033 --> TC_061
    SR_034 --> LLR_045
    SR_034 --> LLR_046
    SR_034 --> LLR_047
    SR_034 --> LLR_048
    SR_034 --> TC_063
    SR_034 --> TC_064
    SR_034 --> TC_065
    SR_034 --> TC_066
    SR_034 --> TC_067
    SR_034 --> TC_068
    SR_034 --> TC_069
    SR_034 --> TC_071
    SR_035 --> LLR_049
    SR_035 --> TC_068
    SR_035 --> TC_070
    SR_035 --> TC_071
    SR_036 --> LLR_050
    SR_036 --> LLR_051
    SR_036 --> LLR_052
    SR_036 --> LLR_060
    SR_036 --> LLR_061
    SR_036 --> LLR_062
    SR_036 --> LLR_063
    SR_036 --> TC_072
    SR_036 --> TC_073
    SR_036 --> TC_074
    SR_036 --> TC_086
    SR_036 --> TC_087
    SR_036 --> TC_088
    SR_036 --> TC_089
    SR_037 --> LLR_053
    SR_037 --> LLR_054
    SR_037 --> LLR_055
    SR_037 --> LLR_056
    SR_037 --> LLR_057
    SR_037 --> TC_075
    SR_037 --> TC_076
    SR_037 --> TC_077
    SR_037 --> TC_078
    SR_037 --> TC_079
    SR_037 --> TC_080
    SR_037 --> TC_081
    SR_037 --> TC_082
    SR_038 --> LLR_058
    SR_038 --> LLR_059
    SR_038 --> TC_083
    SR_038 --> TC_084
    SR_038 --> TC_085
    class SR_024 draft;
    class SR_034 draft;
    class SR_035 draft;
    class SR_036 draft;
    class SR_037 draft;
    class SR_038 draft;
    class LLR_045 draft;
    class LLR_046 draft;
    class LLR_047 draft;
    class LLR_048 draft;
    class LLR_049 draft;
    class LLR_050 draft;
    class LLR_051 draft;
    class LLR_052 draft;
    class LLR_053 draft;
    class LLR_054 draft;
    class LLR_055 draft;
    class LLR_056 draft;
    class LLR_057 draft;
    class LLR_058 draft;
    class LLR_059 draft;
    class LLR_060 draft;
    class LLR_061 draft;
    class LLR_062 draft;
    class LLR_063 draft;
    class LLR_064 draft;
    class TC_001 draft;
    class TC_005 draft;
    class TC_007 draft;
    class TC_011 draft;
    class TC_015 draft;
    class TC_016 draft;
    class TC_019 draft;
    class TC_020 draft;
    class TC_021 draft;
    class TC_024 draft;
    class TC_025 draft;
    class TC_032 draft;
    class TC_033 draft;
    class TC_038 draft;
    class TC_040 draft;
    class TC_042 draft;
    class TC_060 draft;
    class TC_063 draft;
    class TC_064 draft;
    class TC_065 draft;
    class TC_066 draft;
    class TC_067 draft;
    class TC_068 draft;
    class TC_069 draft;
    class TC_070 draft;
    class TC_071 draft;
    class TC_072 draft;
    class TC_073 draft;
    class TC_074 draft;
    class TC_075 draft;
    class TC_076 draft;
    class TC_077 draft;
    class TC_078 draft;
    class TC_079 draft;
    class TC_080 draft;
    class TC_081 draft;
    class TC_082 draft;
    class TC_083 draft;
    class TC_084 draft;
    class TC_085 draft;
    class TC_086 draft;
    class TC_087 draft;
    class TC_088 draft;
    class TC_089 draft;
    class TC_090 draft;
```

## Orphans

None. Full coverage.

## Integrity

None. Ids are unique and well-formed.

## Verification basis (attested vs mechanized)

_Of the SRs reported `Verified`: `Attest` rows rest on a named human's recorded judgment (trust-based — the box can be checked without the work having happened, process.md §4); all others rest on a runnable check._

- Mechanized (Test/Demonstration/Manual/Analysis/Inspection): 25
- Attested (Attest): 0

## Performance budgets (§9 back-links)

5 budget row(s); every Refs resolves to a real SR/LLR/Module.
